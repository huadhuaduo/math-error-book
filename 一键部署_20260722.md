# AI 管理陪练 · 一键部署包

> 2026-07-22 · 所有内容在一个文件里，按顺序执行即可

---

## Step 1：数据库迁移

在低代码平台 PostgreSQL 管理工具中执行以下 SQL：

```sql
CREATE TABLE IF NOT EXISTS coaching_classes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  class_name VARCHAR(100) NOT NULL,
  admin_user_id VARCHAR(50) NOT NULL,
  invite_code VARCHAR(20) UNIQUE NOT NULL,
  project_company VARCHAR(100),
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS class_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  class_id UUID NOT NULL REFERENCES coaching_classes(id) ON DELETE CASCADE,
  user_id VARCHAR(50) NOT NULL,
  joined_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(class_id, user_id)
);

ALTER TABLE coaching_sessions ADD COLUMN IF NOT EXISTS class_id UUID REFERENCES coaching_classes(id);

CREATE INDEX IF NOT EXISTS idx_class_members_user ON class_members(user_id);
CREATE INDEX IF NOT EXISTS idx_class_members_class ON class_members(class_id);
CREATE INDEX IF NOT EXISTS idx_sessions_class ON coaching_sessions(class_id);
CREATE INDEX IF NOT EXISTS idx_classes_invite ON coaching_classes(invite_code);
```

---

## Step 2：替换引擎页（2个文件）

以下文件在本地仓库路径，上传到低代码平台替换对应页面：

| 本地文件 | 低代码平台位置 | 改动内容 |
|---------|---------|---------|
| `低代码平台部署_20260721/scene_TG001_引擎页.html` | TG001对话页 | 语音输入+手机端+数据保护+等待提示 |
| `低代码平台部署_20260721/scene_PR2_引擎驱动版_v7.html` | PR2对话页 | 语音输入+手机端+数据保护 |

---

## Step 3：替换管理看板

| 本地文件 | 低代码平台位置 | 改动内容 |
|---------|---------|---------|
| `低代码平台部署_20260721/管理看板.html` | 管理看板 | 班级筛选+自动总结+分层显示 |

---

## Step 4：上传场景分类页（新页面）

| 本地文件 | 低代码平台路由 | 用途 |
|---------|---------|------|
| `低代码平台部署_20260721/场景分类预览.html` | `/coaching/scenes` | 三大板块备用页 |

---

## Step 5：替换首页组件

用 `低代码平台部署_20260721/首页优化/HomePage.tsx` 替换项目中的 `HomePage.tsx`。

改动：DOM 嵌套从 13 层降到 5 层，新增移动端 hamburger 菜单。

---

## Step 6：替换场景页组件

用 `低代码平台部署_20260721/首页优化/CoachingPage_merged.tsx` 替换项目中的 `CoachingPage.tsx`。

改动：合并原 CoachingPage + 场景分类预览，8 个 section 统一布局，含推荐行、三大板块、Tab 筛选、自评弹窗。

---

## 部署验证

- [ ] 数据库表创建成功
- [ ] TG001 引擎页：语音输入可用、手机端正常、结束对话跳转反馈页
- [ ] PR2 引擎页：同上
- [ ] 管理看板：班级下拉框可见
- [ ] 场景分类页可访问
- [ ] 首页：移动端 hamburger 可展开
- [ ] 场景页：三大板块显示正确、筛选生效、自评弹窗可用

---

## 全部本地文件索引

```
/Users/xzzsw127/Claude code/
├── 低代码平台部署_20260721/
│   ├── 班级管理_数据库迁移.sql
│   ├── scene_TG001_引擎页.html
│   ├── scene_PR2_引擎驱动版_v7.html
│   ├── 管理看板.html
│   ├── 场景分类预览.html
│   └── 首页优化/
│       ├── HomePage.tsx
│       ├── CoachingPage_merged.tsx
│       └── CoachingPage_合并预览.html
├── 练习记录.html
├── 陪练首页.html
├── CoachingPage_合并预览.html
└── 一键部署_20260722.md  ← 你正在看的这个文件
```
