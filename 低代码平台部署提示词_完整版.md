# 低代码平台部署提示词

> 将以下内容粘贴到低代码平台 AI 对话中，它会帮你完成部署和验证。

---

## 背景

我要在低代码平台上部署一个三智能体AI陪练系统。三个智能体分别是：场景生成、对话练习、反馈复盘。系统有两套界面可以跑：

- **方式A（React页面）**：`/scenario/generate` → `/scenario/:id/chat` → `/scenario/:id/feedback`
- **方式B（独立HTML）**：`scene_TG001_准备页.html` → `scene_TG001_引擎页.html` → `scene_TG001_反馈页.html`

先帮我跑通方式B，再验证方式A。

---

## 方式B：独立HTML部署

### 第一步：上传三个文件

把这三个HTML文件作为页面部署到低代码平台。它们已经互相链接好了（准备页的"开始对话"按钮→引擎页，引擎页的"结束对话"→反馈页）。

文件路径：
- `低代码平台部署_TG001/scene_TG001_准备页.html`
- `低代码平台部署_TG001/scene_TG001_引擎页.html`
- `低代码平台部署_TG001/scene_TG001_反馈页.html`

### 第二步：验证AI插件接入（这是最关键的）

引擎页内置了AI驱动模式。打开引擎页后，打开浏览器控制台（F12），看日志：

- 打印 `🤖 AI模式：对话由 mdp_coach_ai_conversation_reply_1 驱动` → AI已接入，正常
- 打印 `📋 本地模式：对话由上下文引擎驱动` → AI未接入，需要修复

**如果打印"本地模式"**：说明 `window.capabilityClient` 不可用。请在引擎页的 `<script>` 标签最开头加上：
```javascript
import { capabilityClient } from '@lark-apaas/client-toolkit';
```
如果HTML不支持import，则改用低代码平台的React页面方式（方式A）。

### 第三步：测试对话

部署完成后：
1. 打开准备页 → 看到NPC-周的头像、情境、挑战、目标
2. 点"开始对话" → 跳到引擎页
3. 在对话框输入："示范区开了一周了，客户情况怎么样？"
4. 观察NPC-周的回复是否跟你的问题相关（不是随机回答）
5. 连续对话5轮以上——确认每一轮都是上下文关联的
6. 点"结束对话" → 跳到反馈页

**验收标准**：NPC-周的回复跟你的提问直接相关。如果你问"客户数据怎么样"，他回答"你来的时候我以为又一个画饼的"——说明用的是fallback正则引擎，AI没接入成功。

---

## 方式A：React三智能体验证

方式B跑通后，验证React版。

### 前置确认

React版的三个页面是否已在项目中：
- `client/src/pages/ScenarioGenerate.tsx`
- `client/src/pages/ScenarioChat.tsx`（已import capabilityClient）
- `client/src/pages/ScenarioFeedback.tsx`（已import capabilityClient）

工具文件是否已部署：
- `client/src/utils/detection-rules.ts`
- `client/src/utils/feedback-assembler.ts`

路由是否已配置：
```tsx
<Route path="/scenario/generate" element={<ScenarioGenerate />} />
<Route path="/scenario/:sceneId/chat" element={<ScenarioChat />} />
<Route path="/scenario/:sceneId/feedback" element={<ScenarioFeedback />} />
```

### 验证步骤

1. 访问 `/scenario/generate` → 看到案例库列表（TG-001等4张卡）
2. 点TG-001 → 进入对话页 → AINPC-周对话
3. 完成对话 → 跳转反馈页 → 看到评分和模式分析
4. 控制台无红色报错

---

## 常见问题速查

| 问题 | 原因 | 修复 |
|------|------|------|
| AI模式不启动，一直走fallback | `capabilityClient`未正确引入 | React版用`import { capabilityClient }`；HTML版确认低代码平台全局注入 |
| 对话页白屏 | JS语法错误 | 检查控制台报错，贴给我 |
| NPC-周回复跟问题不相关 | 正则fallback在跑 | 确认控制台打印的是"AI模式" |
| 反馈页空白 | `feedback-assembler.ts`未部署 | 确认在`client/src/utils/`下 |
| 场景列表为空 | 案例库未导入 | 确认POST `/api/coaching/seed-scenarios`已执行 |

---

## 目标

跑通之后的状态：
- 用户在准备页看到NPC-周 → 点开始 → AINPC-周跟他自由对话 → 对话内容上下文关联 → 完成后看到反馈报告
- 换一个场景（TG-002/TG-003）→ 同样流程，不需要改任何代码
