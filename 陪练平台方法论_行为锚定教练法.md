# 陪练平台方法论：行为锚定教练法

> **版本**：V1.0 · **设计原则**：一套方法论跑通所有管理场景，从行为觉察到业务闭环

---

## 目录

1. [核心问题](#一核心问题)
2. [方法论总览](#二方法论总览)
3. [Layer 1: NPC 自适应引擎](#三layer-1-npc-自适应引擎)
4. [Layer 2: 行为评估引擎](#四layer-2-行为评估引擎)
5. [Layer 3: 反馈诊断引擎](#五layer-3-反馈诊断引擎)
6. [Layer 4: 行动闭环](#六layer-4-行动闭环)
7. [场景设计模板](#七场景设计模板)
8. [跨场景复用指南](#八跨场景复用指南)

---

## 一、核心问题

### 1.1 这个平台要解决什么

管理者在真实工作中面临大量"高难度对话"——绩效反馈、冲突处理、离职面谈、向上汇报。传统的培训方式（听课、案例讨论、角色扮演）有一个根本缺陷：

> **知道 ≠ 做到。** 管理者能在课堂上说出"应该先共情再引导"，但面对一个失望的老员工时，第一反应还是"你觉得第一步可以是什么"。

陪练平台的核心价值是：**在一个安全的、但足够真实的模拟环境中，让管理者暴露真实的行为模式，然后给他看"他做了什么、导致了什么、可以怎么改进"。**

### 1.2 为什么需要一个统一的方法论

如果你的每个场景都用不同的逻辑来设计 NPC 行为、评估管理者表现、生成反馈——那每做一个新场景都是从零开始。一套统一的底层引擎可以让你：

- 新场景只需要填写**场景配置表**（NPC 档案 + 状态机 + 规则），不用重写逻辑
- 反馈的质量和深度保持一致
- 跨场景追踪同一个管理者的行为变化（这个人在"差距明确化"上始终弱于"心理安全营造"吗？）

---

## 二、方法论总览

### 2.1 四个 Layer，一个闭环

```
┌─────────────────────────────────────────────────────────┐
│                    陪练平台核心架构                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Layer 1: NPC 自适应引擎                                  │
│  ┌───────────────────────────────────────────────┐     │
│  │ 状态机 ─→ 规则匹配 ─→ 信息披露 ─→ 响应生成        │     │
│  │   ↑                        ↓                  │     │
│  │   └── 管理者行为输入 ──── NPC 对话输出 ──┘       │     │
│  └───────────────────────────────────────────────┘     │
│                         ↓                               │
│  Layer 2: 行为评估引擎                                    │
│  ┌───────────────────────────────────────────────┐     │
│  │ 规则触发检测 ─→ 状态转换归因 ─→ 多维度评分        │     │
│  └───────────────────────────────────────────────┘     │
│                         ↓                               │
│  Layer 3: 反馈诊断引擎                                    │
│  ┌───────────────────────────────────────────────┐     │
│  │ 摘要卡片 ─→ 行为证据链 ─→ 学习桥接 ─→ 觉察触发   │     │
│  └───────────────────────────────────────────────┘     │
│                         ↓                               │
│  Layer 4: 行动闭环                                       │
│  ┌───────────────────────────────────────────────┐     │
│  │ 微练习 ─→ 再练习触发 ─→ 难度升降 ─→ 行为变化追踪  │     │
│  └───────────────────────────────────────────────┘     │
│                                                         │
│  最终目标：行为改变 → 绩效提升 → 业务影响                   │
└─────────────────────────────────────────────────────────┘
```

### 2.2 设计哲学

| 原则 | 含义 | 来源 |
|------|------|------|
| **行为锚定** | 评"做了什么"不评"想什么"——每条判断必须引用原话 | V2.6 强制行为证据链 |
| **状态可见** | NPC 的状态变化实时可见——不是黑箱 | Edmondson 心理安全 |
| **觉察先于纠正** | 先让管理者看到自己做了什么，再告诉他对错 | Vygotsky ZPD |
| **行动闭环** | 反馈不能停在"知道了"——必须有下一步动作 | Kolb 经验学习循环 |
| **场景可配置** | 同一套引擎适配不同场景——只换配置不换逻辑 | 平台化 |

---

## 三、Layer 1: NPC 自适应引擎

### 3.1 核心逻辑

NPC 的对话**不是预先写好的剧本**。它是根据管理者的实际行为**动态生成**的。

```
管理者输入一句话
    ↓
引擎做三件事：
    ↓
① 行为检测：这句话触发了哪条规则？（R1? P3? F3?）
    ↓
② 状态判定：当前 NPC 在哪个状态？行为是否触发状态转换？
    ↓
③ 响应生成：基于 (当前状态 + 触发规则 + 信息披露层级 + 对话轮次)
    从该状态的响应模式库中选择/生成 NPC 回复
```

### 3.2 NPC 响应模式库（以NPC-张为例）

每个状态有对应的响应模式，不是固定台词：

| 状态 | 响应模式 | 示例（非固定） |
|------|---------|---------------|
| S1 防御 | 测试诚意 | "上次你也这么说，这次有什么不一样？" |
| S1 防御 | 打功劳牌 | "前两周我把数据拉回来了——你看到了吗？" |
| S1 防御 | 被识破循环 | "你又问这个？上次也是这么问的。" |
| S2 松动 | 给入口但要证据 | "你说说看，怎么换？" |
| S2 松动 | 提具体条件 | "你先把我上周四的邮件回了，就回对或不对。" |
| S3 参与 | 主动提方案 | "周三我想带点东西来，你帮我看方向对不对。" |
| S3 参与 | 提需求 | "能不能把那两个新人的调动批了？" |
| S4 承诺 | 确认细节 | "那就定周三下午3点。这次你办公室门是开着的吧？" |
| S5 崩溃 | 放弃沟通 | "没什么好补充的了。" |

### 3.3 为什么能自适应

**关键设计**：NPC 不根据管理者的"话"来回应——而是根据管理者**话里的行为模式**来回应。

```
管理者说："你觉得第一步可以是什么？"

引擎检测到：
- 当前状态：S2（NPC-张刚刚给了一个入口："你先回我邮件"）
- 规则匹配：未检测到 F3（跟进动作），检测到话术循环（同一句式第2次）
- 判定：管理者在用标准话术，没有接NPC-张给的入口

NPC 响应（从 S2 模式库中选择"被忽视入口"模式）：
"你这问题问得跟绕口令似的。我说的不是'第一步'——
我说的是'你先回我上周四的邮件'。就两个字：'对'或者'不对'。"
```

同样的管理者输入，如果当前状态不同、规则匹配不同，NPC 的回应就不同。这就是自适应。

### 3.4 跨场景迁移

换一个场景（比如年轻员工小李、跨部门冲突阿杰），只需要换三样东西：

| 要换的 | 说明 | NPC-张示例 |
|--------|------|---------|
| NPC 档案 | 年龄/资历/痛点/语言风格 | 42岁、8年、努力无回响、说话平实 |
| 状态机定义 | 每个状态的触发条件 | S1防御：测试诚意 → S2松动：要证据 |
| 响应模式库 | 每个状态下的回复模式 | S1有"测试诚意""打功劳牌""识破循环" |

**引擎本身（行为检测→状态判定→响应生成）不变。**

---

## 四、Layer 2: 行为评估引擎

### 4.1 规则系统设计

21 条规则分属 4 个维度。每条规则有明确的可检测标准：

```
规则格式：
[编号] [规则名] [分值范围] [可观察性] [客观判定标准]

示例：
R1 用具体数据呈现差距 +8~12 ✅高
  检测：管理者提到了具体数字（"Q1完成率68%，Q2目前45%"）
  而非模糊判断（"你最近不行"）
```

### 4.2 规则触发检测

引擎在每轮对话后检查所有 21 条规则是否被触发：

```
检测流程：
1. 关键词/模式匹配（如检测到"你觉得...？"→ 可能触发 E1 提问引导）
2. 上下文验证（这句"你觉得...？"是在共情后问的，还是在施压后问的？）
3. 规则得分计算（触发正向→加分，触发负向→扣分）
4. 状态转换判定（当前规则触发的组合是否满足状态转换条件）
```

### 4.3 4 维度评分模型

| 维度 | 初始分 | 理论基础 | 评价对象 | 典型规则 |
|------|--------|---------|---------|---------|
| 差距明确化 | 35 | Locke & Latham | 是否让员工清晰认知差距 | R1-R7 |
| 心理安全营造 | 40 | Edmondson | 是否营造安全氛围 | P1-P7 |
| 赋能发展 | 45 | Bass 变革型领导 | 是否激发自主思考 | E1-E5 |
| 闭环跟进 | 40 | House 路径-目标 | 是否建立跟进机制 | F1-F7 |

**初始分数的依据**（以NPC-张为例）：

- 差距明确化 35：NPC-张认为自己没问题（外部归因），管理者需要从零建立差距认知
- 心理安全营造 40：双方信任已被上次的跟进缺失消耗，需要重建
- 赋能发展 45：NPC-张有经验基础，不是"不会做"而是"不知道方向对不对"
- 闭环跟进 40：对话尚未开始，中等偏低（这类对话最容易"谈完就完了"）

**不同场景的初始分不同**。比如一个年轻员工犯错场景，差距明确化的初始分会更高（事实清楚），但心理安全营造的初始分会更低（年轻人更容易感到被审判）。

---

## 五、Layer 3: 反馈诊断引擎

### 5.1 反馈设计的核心原则

> **反馈的目的不是告诉管理者"你做错了"，是让他看到"他没看到的东西"。**

三条设计原则：
1. **先摘要后详情**：第一眼看到的必须是核心洞察，想深入了解才展开
2. **每条判断必须附证据**：规则编号 + 管理者原话 + NPC 反应 → 三要素缺一不可
3. **反馈的终点是行动**：不能停在"你需要改进XX"——必须给具体的微练习

### 5.2 展开层架构

```
【首屏摘要卡片 ≤200字】
├── 核心洞察（1句话抓住本质）
├── 自评对照（你觉得自己怎么样 vs 数据说你怎么样）
├── 4维度速览（进度条）
└── 最关键改进点（只给1个，不给3个）

  ↓ 点击展开

【展开块1：行为复盘】
├── 状态路径（每个转换标注触发行为 + NPC反应）
├── 5原则归因（每条含规则编号 + 原话 + NPC-张反应 + 业务适切性）
├── 4维度得分 + 一句话画像
└── 做对了 / 可以更好 / 如果重来

【展开块2：学习桥接】
├── 薄弱点判定（规则得分汇总 + 不一致性检测）
└── 知识点桥接（规则 → 关键动作 → KP → 课程，双跳）

【展开块3：下一步行动】
├── 承诺检查（NPC最后的状态是真诚还是虚假）
├── 再练习触发（什么情况→学什么→再练什么→难度怎么调）
└── 反思提示（填空式，不是开放式）
```

### 5.3 觉察触发的时机

对话中嵌入觉察提示（而非只在对话后给反馈），三个触发时机：

| 触发时机 | 频率 | 格式 |
|---------|------|------|
| 每 3 轮（第 3/6/9 轮） | ≥1 条 | "你做了[X]，NPC-张的反应是[Y]。你觉得为什么？" |
| 状态转换时 | ≥1 条 | "NPC-张从防御转为松动——你注意到是什么行为触发的吗？" |
| 行为模式重复时 | ≥1 条 | "你连续3轮用了同样的提问句式——NPC-张在第3轮说'你又问这个'" |

### 5.4 ZPD 自适应配方

同样是"行为循环"，对三个水平的管理者，觉察提示的写法不同：

| 水平 | 配方 | 觉察提示格式 | 示例 |
|------|------|-------------|------|
| A-新手 | 40%觉察+60%复盘 | 直接指出模式 | "你连续3轮用了'你觉得第一步可以是什么'，这导致NPC-张从松动回退到防御。更好的做法是..." |
| B-中等 | 50%觉察+50%复盘 | 提问引导 | "你用了'你觉得第一步可以是什么'3次——NPC-张在第3轮说'你又问这个'。你觉得他为什么会有这个反应？" |
| C-高手 | 70%觉察+30%复盘 | 视角扩展 | "从信任修复的视角看，你重复使用同一个提问句式，意味着你进入了'技术依赖'——用标准流程替代了当下判断。如果换个视角，你会怎么做？" |

---

## 六、Layer 4: 行动闭环

### 6.1 为什么大多数培训没有闭环

```
传统培训：听课 → 记笔记 → 回去上班 → 忘了 → 什么都没变
角色扮演：演练 → 老师点评"不错" → 回去上班 → 遇到真实情况还是不会

核心问题：没有从"知道"到"做到"的桥梁。
```

### 6.2 闭环设计

```
对话练习（暴露真实行为模式）
    ↓
反馈诊断（看到自己没看到的行为）
    ↓
微练习（一个具体、可执行、可验证的小动作）
    ↓
再练习（同一场景或变体场景，检验行为是否改变）
    ↓
难度升级（NPC更难搞、时间更紧、约束更多）
    ↓
行为内化（新行为成为本能反应）
    ↓
业务影响（真实工作中的对话质量提升）
```

### 6.3 再练习触发逻辑

```
情况A：1个维度薄弱 + 其他不薄弱
  → 学针对该维度的课程 → 再练同一场景（同难度）

情况B：≥2个维度薄弱 或 行为不一致（同一KP下规则得分标准差>5）
  → 学最薄弱维度的课程 → 再练同一场景（NPC难度升级）

情况C：所有维度得分>5（无明显薄弱）
  → 学进阶课程 → 换变体场景（不同NPC、不同约束）

情况D：对话失败（NPC进入崩溃态）
  → 学全部核心课程 → 再练同一场景（NPC难度降低）
```

### 6.4 行为变化追踪

同一个管理者在不同时间练习同一场景（或变体场景），追踪：

```
指标1：4维度得分变化（绝对值+变化率）
指标2：状态路径（是否从S4承诺→S5崩溃改善为S1→S2→S3→S4）
指标3：特定规则的触发频率（F3跟进机制是否从0变为稳定触发）
指标4：话术循环次数（是否从5次降低到0次）
指标5：NPC士气终点（是否从0提高到稳定水平）
```

---

## 七、场景设计模板

### 7.1 模板结构

设计一个新场景时，按以下结构填写：

```yaml
# ===== 场景基础信息 =====
scenario:
  id: "S-XXX"
  title: "[场景名称]"
  type: "深度对话型" | "快速判断型" | "多方协调型"
  difficulty: "基础级" | "挑战级" | "专家级"
  rounds: 10-12
  tags: ["带团队", "跨部门", "向上管理", ...]

# ===== NPC 档案 =====
npc:
  name: "[姓名]"
  age: [年龄]
  tenure: "[司龄描述]"
  role: "[角色描述]"
  
  # 核心痛点（为什么会有这场对话）
  pain_points:
    - "[痛点1：不要用标签，用具体困境]"
    - "[痛点2]"
    
  # 语言风格（NPC怎么说话）
  language_style: "[描述语言特征]"
  forbidden_language:
    - "[不能出现的词/句式]"
    
  # NPC 的核心诉求（他真正想要什么）
  core_need: "[一句话]"

# ===== 状态机 =====
states:
  - id: S1
    name: "[初始态名称]"
    description: "[NPC在这个状态下的表现]"
    entry: "对话开始默认"
    exit_conditions:
      - "[管理者做了什么→触发状态转换]"
    response_patterns:
      - "[这个状态下NPC的典型回应模式1]"
      - "[模式2]"
      
  - id: S2
    name: "[第二态名称]"
    # ... 同上

  # ... 继续 S3, S4, S5

# ===== 规则配置 =====
rules:
  dimensions:
    - name: "[维度1名称]"
      initial_score: [初始分数]
      rationale: "[为什么是这个初始分]"
      rules:
        - id: "R1"
          name: "[规则名称]"
          score: "[+/-分数范围]"
          detection: "[如何检测这条规则被触发]"
          
  # ... 继续其他维度和规则

# ===== 信息分层披露 =====
disclosures:
  - level: 1
    rounds: "第1-3轮"
    info: "[基础背景信息]"
    trigger: "开场自动披露"
    
  - level: 2
    rounds: "第4-6轮"
    info: "[第一层隐藏信息]"
    trigger: "[需要管理者做到什么才触发]"
    
  # ... 继续

# ===== 4维度初始分设定 =====
initial_scores:
  dimension_1: [分数]  # [为什么]
  dimension_2: [分数]  # [为什么]
  dimension_3: [分数]  # [为什么]
  dimension_4: [分数]  # [为什么]
```

### 7.2 场景设计原则

1. **NPC 的痛点必须是真实的**——不要编造，从实际管理场景中提取
2. **状态转换条件必须是可检测的**——"管理者态度诚恳"不可检测，"管理者说了'这是我的问题'"可检测
3. **响应模式不预写固定台词**——写的是模式，不是剧本
4. **信息披露要有层次**——NPC 不会一上来就把所有苦水倒出来
5. **初始分数要有场景依据**——不同场景同一个维度的初始分可以不同

---

## 八、跨场景复用指南

### 8.1 哪些是不变的（引擎层）

| 组件 | 复用方式 |
|------|---------|
| 状态机引擎 | 所有场景统一使用。换状态定义，不换引擎。 |
| 规则检测引擎 | 所有场景统一使用。换规则定义，不换检测逻辑。 |
| NPC 响应生成逻辑 | 所有场景统一使用。换响应模式库和 NPC 档案。 |
| 行为评估引擎 | 所有场景统一使用。换维度定义和初始分。 |
| 反馈诊断架构 | 所有场景统一使用。摘要→证据→桥接→行动的展开结构不变。 |
| ZPD 自适应 | 所有场景统一使用。A/B/C 三档配方适配所有场景。 |

### 8.2 哪些是要换的（配置层）

| 配置项 | 说明 |
|--------|------|
| NPC 档案 | 每个场景一个 |
| 状态机定义 | 每个场景的状态数量和转换条件不同 |
| 规则集 | 不同场景可能侧重不同的维度和规则 |
| 响应模式库 | 每个 NPC 的语言风格和回应模式不同 |
| 信息披露表 | 每个场景的信息层次和触发条件不同 |
| 4维度初始分 | 每个场景的起点不同 |

### 8.3 从NPC-张到其他场景的迁移路径

```
Step 1: 复制场景配置模板
Step 2: 填写新 NPC 档案（痛点→语言风格→核心诉求）
Step 3: 定义新状态机（S1-S5 各自的触发和表现）
Step 4: 配置规则权重（如果4维度适用，只需调整初始分；如果不适用，新增维度）
Step 5: 设计信息披露层次
Step 6: 编写 NPC 响应模式库
Step 7: 跑试跑→调整→再试跑→确认
```

### 8.4 场景分类建议

| 场景类型 | 示例 | NPC 状态特点 | 对话难度 |
|---------|------|-------------|---------|
| 绩效反馈 | 老员工下滑 / 新人未达标 | 防御→松动→参与→承诺 | 中-高 |
| 冲突调解 | 跨部门冲突 / 团队内部矛盾 | 对立→倾听→折中→共识 | 高 |
| 离职面谈 | 主动离职挽留 / 被动离职沟通 | 决绝→松动→重新考虑 / 防御→接受 | 高 |
| 向上汇报 | 争取资源 / 汇报坏消息 | 紧张→试探→获得支持 | 中 |
| 日常辅导 | 新人带教 / 技能纠正 | 困惑→理解→尝试 | 低-中 |

---

## 附录 A：NPC-张场景完整配置（范本）

见 [场景_P1_完整方案_v6.md](场景_P1_完整方案_v6.md)

## 附录 B：文件清单

| 文件 | 类型 | 用途 |
|------|------|------|
| `陪练平台方法论_行为锚定教练法.md` | 本文档 | 方法论总纲 |
| `场景设计模板_空白版.md` | 模板 | 新场景配置模板 |
| `场景_P1_完整方案_v6.md` | 范本 | NPC-张场景完整配置 |
| `scene_P1_preparation_v7.html` | HTML | 准备页 |
| `scene_P1_dialogue_v2.html` | HTML | 对话页 |
| `scene_P1_feedback_v2.html` | HTML | 反馈诊断页 |

---

*文档版本：V1.0 | 适用平台：任意低代码平台（可直接上传 HTML + 读取 MD 配置）*
