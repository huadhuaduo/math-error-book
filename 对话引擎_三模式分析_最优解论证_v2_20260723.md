# 对话引擎深度分析 v2：120人并发假设下的最优解

> 2026-07-23 · 基于新信息重做：8000人总量、训战陪练单场可达120人同时练习
> v1 的核心错误：假设了"3人同时练习"的低并发场景，从而得出"AI优先"的结论。这在管理陪练中成立，在训战陪练中不成立。

---

## 纠正：AI优先只对管理陪练有效

### 120人同时练习的并发模型

```
训战陪练场景（MTP课后，120人同时开始）:

Minute 0: 120人同时点"开始对话"
Minute 0-1: 120人发送第一条消息（"你好NPC-周"/"最近项目怎么样"等）
              → 如果AI优先：120个并发AI请求 → 基于实测数据推算：
                20并发=67%成功 → 120并发 ≈ 30-40%成功率
              → 60-80人看到AI失败 → 降级到回应池
              → 但用户感知是"NPC-周反应好慢"或"NPC-周不理我"

Minute 1-2: 120人发送第二条消息
              → 第一轮AI失败的用户的第二轮请求叠加第一轮重试
              → 并发可能达到150+
              → 成功率继续下降
              → 恶性循环
```

**结论：训战陪练模式下，AI优先是灾难。120人同时练习时，AI插件不可用。**

### 但管理陪练的并发模型完全不同

```
管理陪练场景（个人自主练习，不同时间打开）:

工作日9:00-18:00，假设100人自主练习，均匀分布:
  9小时内 = 540分钟，100人 = 每5.4分钟有1人开始练习
  每人10轮对话，每轮间隔5-30秒
  峰值并发: 最坏情况2-5个请求同时到达
  AI成功率: 接近100%（5并发远低于20并发的崩溃点）
```

**结论：管理陪练模式下，AI优先是正确的。5个并发不会触发AI插件瓶颈。**

### 这就是为什么需要 dialogue_mode

不是"三种模式换三种侧边栏"。是**三种模式决定了对话引擎的AI调用策略**：

| | 管理陪练(narrative) | 训战陪练(training) | 业务陪练(drill) |
|---|---|---|---|
| **并发假设** | 低（<10） | **高（100+）** | 中（<50） |
| **AI调用策略** | **AI优先** | **回应池优先，AI不参与实时对话** | **回应池优先** |
| **回应池要求** | 降级安全网（够用就行） | **核心引擎（必须精准）** | 核心引擎 |
| **缓存策略** | 单用户localStorage | **共享缓存+预热** | 单用户localStorage |

---

## 训战陪练：回应池从"降级方案"升级为"核心引擎"

### 当前回应池为什么不够

54条回应 × 9种意图 = 每(intent, mood)约1.5条。120人同时用，每人10轮=1200次请求。1200 / 54 ≈ 22次重复。同一句回应被20+人看到——而且是同班同学，互相能交流。

### 训战陪练回应池需要什么

120人场景下，回应池不是"AI的降级方案"——它是**唯一的实时回应来源**。需要：

**1. 数量：至少5-6变体/意图**
- 120人 × 10轮 = 1200次请求
- 假设20个(intent, mood)组合覆盖80%请求 = 960次
- 960 / 20 = 48次重复/组合
- 5个变体 × 轮换 = 每人看到约10次重复——勉强可接受
- 6个变体 = 每人看到约8次重复——更好

**2. 匹配精度：意图分类需要更细粒度**
- 当前9种意图对120人不够——"talk_biz"可能覆盖了40%的请求
- 训战陪练需要**MTP知识点触发检测**：不是分类用户意图，是检测用户是否应用了特定MTP概念
- "在做减法"vs"给了对比表"→ 这不是意图分类能区分的，需要**知识点匹配**

**3. 预热缓存：课前预加载**
- 120人在同一教室、同一时间打开同一场景
- 如果场景卡和回应池在课前就加载到每个用户的localStorage
- 第一次请求就不需要网络调用
- 低代码平台上：场景卡JSON + 回应池 = 约50KB，localStorage够用

**4. 共享缓存：如果有后端**
- 同一场景、同一轮次、同一意图 → 回应应该可以跨用户共享
- 但低代码平台上可能没有Redis → 退一步：用localStorage单用户缓存

### 训战陪练的引擎流程

```
训战陪练对话引擎（120人同时）:

用户输入
    │
    ▼
L1: localStorage缓存查找
    key = sceneId + round + intent + fingerprint
    ├── 命中 → 即时返回（0ms）
    └── 未命中 ↓
    
L2: 回应池匹配（增强版）
    ├── 意图分类（9类→细化为~20类，加入MTP知识点维度）
    ├── NPC状态匹配
    ├── 变体轮换（至少5变体，跨用户不重复——需要随机种子）
    ├── 命中 → 返回 + 写入L1缓存（200-500ms延迟模拟思考）
    └── 未命中 ↓

L3: 知识提示兜底
    "NPC-周看着你，等你说下去。" + 引导者提示
    例："💡 MTP3·8：非权力影响力来自专家权+典范权。
          试试问NPC-周'你觉得哪两个户型值得主力推？'"
    → 学员得到提示，继续对话
    → 不调AI，不阻塞

本轮结束。AI不参与实时对话。
```

**关键改变：训战陪练模式下，AI完全不参与实时对话。** 对话质量完全依赖回应池+引导者提示。AI只用于课后——反馈生成（分析对话记录、识别知识点薄弱点、生成老师信号）。

---

## 管理陪练：AI优先，但需要更聪明的降级

### 管理陪练的AI调用策略不变

管理陪练的并发假设仍然是低（<10人同时）。AI优先是正确的。

```
管理陪练对话引擎（<10人同时）:

L1: localStorage缓存
L2: AI(3秒超时) ← systemPrompt来自场景卡
L3: 回应池降级 ← 精度不够但不会断
L4: fallback
```

### 但训战陪练的分析暴露了一个问题

管理陪练的回应池也是54条——如果未来扩展到20-30人同时自主练习，同样会遇到精准度问题。管理陪练的回应池也需要从"降级安全网"升级到"靠谱的备用引擎"。

**管理陪练回应池需要：**
- 每个(mood, intent)至少4变体（当前3变体）
- WATCH/TEST/VERIFY/FOLLOW 四种状态的关键转折点各加2条"分叉回应"——覆盖学员选择了不同路径的情况

---

## 业务陪练：回应池 + 脚本合规检测

业务陪练（销售/客服）的并发假设居中（<50人）。但业务陪练的特征是**高度结构化**——有标准话术、有步骤流程。

```
业务陪练对话引擎:

L1: localStorage缓存
L2: 回应池（话术步骤匹配）
    ├── 检测学员是否完成了当前步骤的必要话术元素
    ├── 如果完成 → 推进到下一步
    ├── 如果遗漏 → 引导者指出："标准话术中这一步需要提到{missing_element}"
    └── NPC回应基于当前步骤的标准客户反应
L3: AI(仅在话术步骤全部完成后，做综合分析)
```

业务陪练的回应池和训战陪练不同——它是**按脚本步骤组织的**，不是按NPC状态。每个步骤有5-8个标准客户回应变体。

---

## 修正后的最终架构

```
对话智能体
│
├── respond(sceneId, userMsg, history, context)
│   │
│   ├── [training mode] 训战陪练（高并发）
│   │   L1: 缓存 → L2: 增强回应池(5-6变体+MTP知识点匹配) → L3: 知识提示兜底
│   │   AI: 不参与实时对话，仅用于课后反馈生成
│   │
│   ├── [narrative mode] 管理陪练（低并发）
│   │   L1: 缓存 → L2: AI(3s超时) → L3: 回应池(4变体) → L4: fallback
│   │   AI: 实时对话核心引擎
│   │
│   └── [drill mode] 业务陪练（中并发）
│       L1: 缓存 → L2: 回应池(按脚本步骤) → L3: AI(步骤完成后综合分析)
│       AI: 辅助分析，不参与实时对话
│
├── getFacilitatorHints(userMsg, config, ctx)
│   ├── training → 知识提示（MTP知识点触发）
│   ├── narrative → 轻声提醒（管理陷阱检测）
│   └── drill → 话术对比（遗漏元素提醒）
│
└── detectEvents(userMsg, config, ctx)
    ├── training → 知识点遗漏检测 + 决策点插入
    ├── narrative → 陷阱检测 + NPC状态推进
    └── drill → 脚本合规检测 + 步骤推进
```

---

## 对反馈引擎的影响

反馈AI（`ai_coach_analysis_report_1`）不受120人并发影响——因为：

1. 反馈在对话结束后才调用，不是实时
2. 120人的反馈请求会在10分钟内分散到达（每人结束时间不同）
3. 平均每分钟12次反馈AI调用——这是安全的

**反馈引擎的分析结论不变：判词+觉察用AI，行为对照用L2，其余L1。**

---

## 对实施计划的影响

实施计划不变，但步骤3（对话引擎重构）需要额外工作：
- 训战陪练的增强回应池：从54条扩展到100+条（5-6变体/意图 × 20个细粒度意图）
- MTP知识点触发检测：新增`knowledge_points`字段的匹配逻辑
- 管理陪练回应池：从3变体扩展到4变体/意图

步骤3的预估从1天调整为1.5天。总工作量约4.5天。
