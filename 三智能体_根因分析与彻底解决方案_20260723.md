# 三智能体架构根因分析与彻底解决方案

> 2026-07-23 · 基于完整代码审查 + 历史对话追溯 + AI并发实测数据

---

## 第一部分：根因分析

### 一、症状链

```
用户看到的症状：
  对话回复不精准 → 同一回复 → 提示不触发 → 反馈模板化 → 测试失败
                              ↑
代码层面的直接原因：
  TG001引擎页(528行) 硬编码了NPC-周的NPC逻辑、陷阱、回应池
  PR2引擎页(另一套代码) 硬编码了管理者B的NPC逻辑
  tg001-feedback-logic.ts(335行) 硬编码了管理场景的6个模块
  三个智能体的"三"只存在于文档里，不存在于代码中
                              ↑
架构层面的根因：
  没有"场景卡驱动引擎"的机制 — 场景卡JSON有18个字段但引擎只读了5个
  没有"引擎间合约" — 对话引擎不知道反馈引擎需要什么数据
  没有"模式分发" — narrative/training/drill 三种模式从未进入代码
                              ↑
流程层面的根因：
  7/18设计了完整的三模式架构 + 组件化反馈
  7/21写入了行动清单 #15 #16 #17
  但此后所有开发都在TG001的单文件上修bug
  架构设计和代码实现是两条平行线
```

### 二、逐文件追踪：为什么架构设计没有进入代码

**1. 场景卡数据协议（场景卡数据协议_三智能体合约.md）定义了18个字段。**
打开TG001的场景卡JSON和引擎代码，做字段对账：

| 协议字段 | 在TG001场景卡JSON中 | 在引擎代码中被读取 | 状态 |
|---------|-------------------|------------------|------|
| scene_id | ✅ | ✅ | |
| npc_name | ✅ | ✅ | |
| system_prompt | ❓ | ✅ (硬编码在getTG001SystemPrompt()) | 协议定义了但代码没用JSON的值 |
| dialogue_mode | ❌ 协议里没有 | ❌ 代码里也没有 | **7/18设计的字段从未进入协议** |
| learning_theory | ❌ | ❌ | 同上 |
| traps | ❓ | ✅ (硬编码在detectTraps()) | JSON可能有但代码不用 |
| success_criteria | ❓ | ❌ 代码硬编码了维度标签 | **这就是为什么反馈页模板化** |
| prompt_rules | ❌ | ❌ | 引导逻辑散落在detectTraps/showWhisper里 |

**根因1：场景卡协议不完整。** 7/18设计的三个关键字段(`dialogue_mode`, `learning_theory`, `prompt_rules`)从未进入协议。引擎代码不知道这些字段存在，自然不会按模式分发。

**2. 对话引擎入口不统一。**
TG001引擎页的入口是 `TG001EnginePage.tsx` 里的 `sendMsg()` 函数（第100行）。它直接调用 `bRouteRespond()` — 传入硬编码的 `getTG001SystemPrompt()`。PR2引擎页有另一套sendMsg逻辑。不存在一个"通用对话引擎"。

**根因2：没有引擎抽象层。** 每个场景的对话页是一个独立的React组件，各自维护sendMsg逻辑。新增场景=复制粘贴一个500行的tsx文件然后改里面的NPC名字。

**3. 反馈逻辑硬编码了管理场景。**
`tg001-feedback-logic.ts` 的所有函数（getKeyMoment/getVerdict/getManagementInertia/getBehaviorSteps）的文本输出是为管理陪练场景写的。MTP训后场景想要"知识点掌握清单+老师信号"——但当前代码没有这个分支。

**根因3：反馈引擎不知道场景类型。** `getBehaviorSteps` 输出"4步行为对照"——这是管理场景的框架。MTP训后场景应该是"培训要点Checklist"。但反馈逻辑没有 `switch(sceneConfig.learning_theory)` — 它根本不知道有其他场景类型。

### 三、为什么修了无数次还修不好

每一次"修对话引擎"实际上是在做什么？

```
第一次修：NPC-周回复太短 → 改systemPrompt → 部署 → TG001好了
第二次修：NPC-周回复不精准 → 加回应池 → 部署 → TG001好了，但PR2没回应池
第三次修：反馈页模板化 → 改feedback-logic.ts的预写文本 → TG001反馈好了，但PR2反馈还是老的
第四次修：上下文Bug（重复回复）→ 检查bRouteRespond → 发现是conversationHistory没更新 → 修复
第五次修：反馈页数据链路断裂 → 检查endConversation → 发现没写localStorage → 修复
```

每次修复的生效范围：**一个场景的一个文件**。没有一次修复能同时解决TG001和PR2的问题——因为它们是两套代码。

**根因4：修复的爆炸半径=1。** 没有共享引擎，每个修复只影响当前场景。下次其他场景出现类似问题时，需要重新修复。

---

## 第二部分：彻底解决方案

### 核心原则

**场景卡JSON是唯一真相源（Single Source of Truth）。** 对话引擎、引导者、反馈引擎的行为全部由场景卡JSON中的字段驱动。代码只负责"读取配置并执行"——不包含任何场景相关的硬编码。

### 协议设计：场景卡 v2.0

```typescript
// 场景卡协议 v2.0
interface ScenarioCard {
  // === 基础信息 ===
  scene_id: string;           // "TG-001"
  scene_name: string;         // "首开倒计时——空降者的20天"
  npc_name: string;           // "NPC-周"
  npc_role: string;           // "项目骨干·10年"
  
  // === 模式定义（7/18设计，现在进入协议）===
  dialogue_mode: "narrative" | "training" | "drill";
  // narrative: NPC情绪状态机 + 自由对话 + 轻声提醒
  // training: 知识点触发 + 决策点插入 + 知识提示
  // drill: 话术匹配 + 合规检查 + 步骤进度
  
  learning_theory: "constructivism" | "knowledge_reinforcement" | "behaviorism";
  // constructivism: 反馈聚焦觉察+行为改变连接
  // knowledge_reinforcement: 反馈聚焦知识点掌握+薄弱点+老师信号
  // behaviorism: 反馈聚焦话术完成率+步骤合规
  
  // === 引擎驱动配置 ===
  system_prompt: string;      // AI NPC角色定义
  
  response_pool: {            // 静态回应池（AI降级用）
    [npcMood: string]: {
      [intent: string]: Array<{
        text: string;         // NPC回应文本
        anchor: string;       // 行为锚点标签（关联success_criteria维度）
      }>
    }
  };
  
  prompt_rules: {
    // 陷阱检测规则
    traps: Array<{
      id: string;             // "T01"
      name: string;           // "先发制人"
      pattern: string;        // 正则表达式字符串
      condition: string;      // 触发条件（JavaScript表达式字符串，如"!ctx.bizMentioned"）
      score: number;          // -15
      dim: string;            // 影响维度："p2_trust"
      msg: string;            // 提示文本
    }>;
    
    // 轻声提醒规则（引导者介入）
    whispers: Array<{
      id: string;
      pattern: string;        // 触发条件
      type: "info" | "warn";  // 提示类型
      msg: string;            // 提示文本
      delay_ms: number;       // 延迟显示
    }>;
    
    // 上下文推进规则
    context_vars: Array<{
      name: string;           // "bizMentioned"
      pattern: string;        // 正则
    }>;
    
    // 自动推进规则（防卡死）
    auto_advance: Array<{
      round: number;
      condition: string;      // JavaScript表达式
      set_var: string;
    }>;
  };
  
  // === 成功标准（反馈引擎驱动）===
  success_criteria: Array<{
    key: string;              // "p2_trust"
    label: string;            // "先做事，不说话"
    benchmark: number;        // 80
    color: string;            // "#F59E0B"
    mtp_anchor: string;       // "信任方程式·降低自我导向"
    behavior_steps: Array<{   // 行为对照步骤
      step: number;
      label: string;
      good_condition: string;  // JavaScript表达式
      bad_condition: string;
      npc_reaction_good: string;
      npc_reaction_bad: string;
      best_practice: string;
      why_good: string;
      why_bad: string;
    }>;
  }>;
  
  // === 反馈模块组 ===
  feedback_modules: string[];
  // 管理陪练: ["verdict", "awareness", "behavior_steps", "next_practice", "knowledge_gaps", "action_plan"]
  // 训战陪练: ["knowledge_checklist", "weak_points", "teacher_signal", "next_practice"]
  // 业务陪练: ["script_compliance", "step_accuracy", "objection_handling", "next_drill"]
  
  // === MTP训后特有 ===
  knowledge_points?: Array<{
    id: string;
    label: string;
    mtp_module: string;
    mtp_anchor: string;
    trigger_pattern: string;  // 检测学员是否应用了该知识点的正则
  }>;
  
  // === 业务陪练特有 ===
  script_steps?: Array<{
    step: number;
    name: string;
    required_elements: string[];
    standard_script: string;
  }>;
}
```

### 引擎接口设计

```typescript
// ===== 对话引擎：唯一入口 =====
// 文件：client/src/agents/dialogue-agent/engine.ts

interface DialogueEngine {
  // 核心方法：所有场景共用
  respond(
    sceneConfig: ScenarioCard,     // 场景卡（纯数据）
    userMsg: string,               // 用户输入
    history: HistoryEntry[],       // 对话历史
  ): Promise<{
    response: string;              // NPC回应
    layer: 'L1' | 'L2' | 'L3' | 'L4';
    events: DialogueEvent[];       // 触发的事件（陷阱/提示/状态变化）
  }>;
}

// 引擎内部流程:
// 1. 从sceneConfig.prompt_rules.context_vars更新上下文
// 2. 从sceneConfig.prompt_rules.traps检测陷阱
// 3. 从sceneConfig.prompt_rules.auto_advance检查自动推进
// 4. L1缓存 → L2 AI(3s超时) → L3 sceneConfig.response_pool → L4 fallback
// 5. 返回回应 + 事件列表


// ===== 引导者：独立模块 =====
// 文件：client/src/agents/dialogue-agent/facilitator.ts

interface Facilitator {
  getHints(
    sceneConfig: ScenarioCard,
    userMsg: string,
    context: Record<string, unknown>,
    round: number,
  ): Hint[];
}

// 内部分发：
// if (sceneConfig.dialogue_mode === 'narrative') → 轻声提醒逻辑
// if (sceneConfig.dialogue_mode === 'training') → 知识提示逻辑
// if (sceneConfig.dialogue_mode === 'drill') → 话术对比逻辑


// ===== 反馈引擎：唯一入口 =====
// 文件：client/src/agents/feedback-agent/generator.ts

interface FeedbackEngine {
  generate(
    sceneConfig: ScenarioCard,
    sessionData: SessionData,
  ): {
    modules: FeedbackModule[];     // 按sceneConfig.feedback_modules顺序生成
  };
}

// 内部分发：
// 读sceneConfig.learning_theory + sceneConfig.feedback_modules
// → constructivism: 加载constructivism模块组
// → knowledge_reinforcement: 加载knowledge-reinforcement模块组
// → behaviorism: 加载behaviorism模块组
```

### 对话页和反馈页的通用化

```typescript
// ===== 通用对话页 =====
// 文件：client/src/pages/DialoguePage.tsx
// 路由：/dialogue/:sceneId

function DialoguePage() {
  const { sceneId } = useParams();
  const [sceneConfig, setSceneConfig] = useState<ScenarioCard | null>(null);
  
  useEffect(() => {
    // 从场景卡JSON加载配置
    fetchSceneCard(sceneId).then(setSceneConfig);
  }, [sceneId]);
  
  if (!sceneConfig) return <Loading />;
  
  // 所有场景共用同一个sendMsg逻辑
  const sendMsg = async (text: string) => {
    const result = await dialogueEngine.respond(sceneConfig, text, history);
    // ...
  };
  
  // 根据dialogue_mode切换侧边栏组件
  const SidePanel = sceneConfig.dialogue_mode === 'narrative' ? NarrativePanel 
    : sceneConfig.dialogue_mode === 'training' ? TrainingPanel 
    : DrillPanel;
  
  return (
    <ChatLayout>
      <ChatArea ... />
      <SidePanel config={sceneConfig} ... />
    </ChatLayout>
  );
}


// ===== 通用反馈页 =====
// 文件：client/src/pages/FeedbackPage.tsx
// 路由：/feedback/:sceneId/:sessionId?

function FeedbackPage() {
  const { sceneId } = useParams();
  const [sceneConfig, setSceneConfig] = useState<ScenarioCard | null>(null);
  const [sessionData, setSessionData] = useState<SessionData | null>(null);
  
  useEffect(() => {
    fetchSceneCard(sceneId).then(setSceneConfig);
    loadSession(sessionId).then(setSessionData);
  }, [sceneId]);
  
  if (!sceneConfig || !sessionData) return <Loading />;
  
  // 反馈引擎生成所有模块
  const modules = feedbackEngine.generate(sceneConfig, sessionData);
  
  // 渲染模块组 — 按sceneConfig.feedback_modules的顺序
  return (
    <FeedbackLayout>
      {modules.map(m => renderModule(m, sceneConfig, sessionData))}
    </FeedbackLayout>
  );
}
```

---

## 第三部分：实施计划

### 总原则

1. **先建合约，再迁代码。** 先把场景卡协议 v2.0 定稿，再按新协议重构引擎。
2. **一次迁移一个场景。** 先用 TG001 验证新架构完整跑通，再迁移 PR2。
3. **每步有验证标准。** 不是"看起来好了"，是数据通过验证。

### 步骤

| 步 | 内容 | 输入 | 输出 | 验证标准 | 预估 |
|----|------|------|------|---------|------|
| **1** | **场景卡协议 v2.0 定稿** | 当前协议 + 7/18设计 + 行动清单#15#16#17 | `场景卡数据协议_v2.0.md` + TypeScript接口定义 | 每个字段有明确的"谁来读、什么时候读、读不到时的默认行为" | 半天 |
| **2** | **TG001 场景卡按 v2.0 补全** | 当前TG001场景卡JSON + 引擎代码中的硬编码值（systemPrompt/traps/whispers/context_vars/success_criteria） | `TG-001_v2.0.json` | 用脚本验证：json的每个字段都能被新引擎接口消费，无undefined | 半天 |
| **3** | **对话引擎重构为配置驱动** | TG001EnginePage.tsx(528行) + tg001-b-route.ts(131行) + tg001-engine-logic.ts(上下文、陷阱) | `dialogue-agent/engine.ts`(~200行) + `dialogue-agent/facilitator.ts`(~100行) | 用TG001场景卡v2.0驱动，对话行为和重构前一致（同样的输入→同样的输出） | 1天 |
| **4** | **反馈引擎重构为配置驱动+模块化** | tg001-feedback-logic.ts(335行) | `feedback-agent/generator.ts` + `feedback-agent/modules/constructivism.ts` + `feedback-agent/modules/knowledge-reinforcement.ts` | 用TG001场景卡v2.0驱动，反馈输出和重构前一致 + 新增原话引用(L2) | 1天 |
| **5** | **反馈AI插件接入（判词+觉察）** | ai_coach_analysis_report_1 插件 | 反馈模块中判词和觉察由AI生成，其他模块保持规则引擎 | AI成功→个性化文本；AI失败→降级到L2模板+原话引用 | 半天 |
| **6** | **对话页/反馈页通用化** | TG001EnginePage.tsx + TG001FeedbackPage.tsx | `DialoguePage.tsx` + `FeedbackPage.tsx`(通用) | `/dialogue/TG-001`加载TG001场景卡，对话和反馈正常 | 半天 |
| **7** | **TG001端到端回归测试** | 全部重构代码 | 测试报告 | 准备页→对话(AI+回应池+fallback三层都触发过)→反馈(判词+觉察AI生成+行为对照L2)→数据保存→练习记录 | 半天 |
| **8** | **真人试跑** | 3位真实管理者 | 测评数据 | NPS≥40 / 反馈页被评为"跟我的对话有关" | 本周 |

**总工作量：约4天。**

### 为什么这个方案不会重蹈覆辙

| 之前的问题 | 这次怎么避免 |
|-----------|------------|
| 修了TG001，PR2又坏了 | 引擎是共享的，改引擎=所有场景受益。场景差异在JSON里 |
| 反馈页永远模板化 | AI模块负责判词+觉察；规则模块有原话引用(L2)；每个模块有明确的责任边界 |
| 新增场景=复制粘贴500行代码 | 新增场景=新建一个JSON文件。引擎不需要改 |
| 架构设计停在纸上 | 场景卡协议 v2.0 = 执行合约。引擎强制读协议，不读硬编码值 |
| 反复修复同一类bug | 每个bug的根因追溯到这里：是协议缺了字段？是引擎没读？是模块没注册？而不是"TG001那个文件又坏了" |

### 风险点

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 场景卡v2.0补全TG001时发现协议设计有漏洞 | 中 | 协议返工半天 | 步骤1-2连续做，不在中间插入其他工作 |
| 重构后对话行为与重构前有差异 | 中 | 用户感知NPC"变了" | 步骤3用A/B对比：同一个history输入，新旧引擎输出是否一致 |
| AI反馈插件实际效果不如预期 | 低 | 判词/觉察质量差 | 降级到L2（已实现），AI作为增强层不影响功能底线 |
| 低代码平台上通用路由不支持 `/dialogue/:sceneId` | 中 | 通用对话页方案需调整 | 步骤6前先在低代码平台上验证路由机制 |
