# 低代码平台平台能力确认

> 发给低代码平台技术支持，确认三项能力边界。回复结果直接影响架构设计。

---

我们的应用场景：
- 陪练平台，8000 人规模
- 三种练习模式：管理陪练（个体自主）、训战陪练（课后集中练习，单场可达 120 人同时开始）、业务陪练（项目集中，50 人以内）
- 三个 AI 插件：`mdp_coach_ai_conversation_reply_1`（对话）、`ai_coach_analysis_report_1`（反馈分析）、`scenario_card_smart_generate_v19_1`（场景生成）

---

### 问题 1：AI 插件是否支持选择不同模型？

对话 AI 需要快速响应（对话实时性要求高，3 秒内），反馈 AI 需要精准分析（可以等 5-10 秒）。如果两个插件可以选不同模型——对话用轻量模型、反馈用量模型——架构可以更灵活。

**具体问：**
- 三个 AI 插件各自用的是什么模型？固定的还是可选的？
- 如果可选，有哪些模型选项？是否有轻量/标准/重量三档？
- 对话插件的 QPS 上限是多少？反馈插件的 QPS 上限是多少？

---

### 问题 2：低代码平台有没有跨用户共享数据的 API？

120 人同时练习同一个场景，如果第一个人调 AI 的回应能缓存下来给后面的人用，AI 并发压力就大幅下降。

**具体问：**
- 有没有 Redis、KV 存储、或任何服务端缓存 API？
- 有没有跨用户共享状态的机制（例如：全局变量、服务端 session）？
- 如果没有现成的 API，是否有变通方案（例如：通过数据库表做临时缓存、通过后端 API 端点做中转）？

---

### 问题 3：前端路由是否支持动态参数？

我们有 TG-001、PR-002、TG-002、TG-003 等多个场景，后续会扩展到十几个。每个场景的对话页和反馈页是同一个组件，只是加载不同的场景配置 JSON。理想情况是 `/dialogue/:sceneId` 和 `/feedback/:sceneId` 这种通用路由。

**具体问：**
- React Router 的动态路由（如 `/dialogue/:sceneId`）在低代码平台上是否支持？
- 还是必须每个场景手动注册独立路由（如 `/tg001/dialogue`、`/pr002/dialogue`）？
- 如果是后者，创建新路由的流程是什么？是否支持批量注册？

---

以上三个问题的答案会直接影响我们的架构方案。如果方便，希望能尽快回复。
