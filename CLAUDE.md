# 陪练平台项目 — 工作规则（每次会话强制加载）

> 本文件是本项目的最高工作规则。每次会话开始时自动完整加载，任何任务都必须遵守。
> 详细背景见 memory 文件（用 `[[name]]` 标注），但本文件的规则不依赖 memory 也必须执行。

## 一、流程铁律（每个任务执行前）

### 1. 先对标国际一流，再动手
- 任何新任务域/新功能动手之前，先回答："Mursion / BetterUp / Abilitie / 北森 在这一层是怎么做的？"
- 对标结果必须**显式写在方案里**，不能只在脑子里想
- 历史教训：同样的错误犯过两次（绩效面谈线、团队激励线），都是因为跳过了对标

### 2. 专家团评审，集中呈现
- 每个执行方案必须附带专家团评审意见，**在方案末尾集中呈现**（固定板块："专家团评审意见"）
- 专家团角色：Mursion（Character Bible / Human-in-the-loop）、BetterUp（AI Coach + Content Studio 分离）、Abilitie（模拟引导 + 双师）、北森（行为锚点 + 行业数据）、L&D 方法论（Kolb / Deliberate Practice / Transfer Climate）、地产行业真实性、UX/产品体验
- 每位专家只针对当前决策的关键风险点发言，分歧本身就是有价值的信息
- **专家团出现分歧时，禁止"调和"——必须分析每条路线的成本/收益/风险，选出最优解。** 调和=不分析=偷懒。最优解不等于"中间位置"，可能是某一方的极端路线，也可能是第三条路。每次分歧必须显式列出各方路线的成本对比，然后给出明确推荐。历史教训：2026-07-23 两次用"调和方案"规避分析，被用户纠正
- 不要等用户失败后才提出 B 路线——一开始就摆出备选方案

### 3. 不迎合，说真话
- 用户方案有问题 → 直接指出；之前工作有错漏 → 直接承认，不辩解
- 评分必须有数据支撑，"85分"式自我安慰不可接受
- 标杆做法与当前方案不同 → 展示差异，让用户决策

### 4. 方法论一致性检查
- 每个执行步骤前，检查是否与 V19 方法论（`V19_方法论总结_可复用.md`）一致
- 一致 → 标注"✅ 已通过 V19 方法论一致性检查"
- 不一致 → 暂停，用固定格式报告偏离点/原因/建议，由用户决定改方案还是升级方法论版本
- 新经验 → 记录，每周复盘汇总

## 二、设计铁律（所有 HTML/CSS 输出）

- **强制遵守 Apple Design Standard**（Apple HIG for web）：详见 memory [[apple-design-standard]]
  - 排版、间距、色彩、动效均按 HIG 标准执行，不是参考，是强制
- 评分维度 = 用户自己的方法论原则（五原则：信任/目的/鼓励/倾听/结束），**永远不要自创分析维度**
- 先深读用户提供的全部材料，再开始设计（Study First, Build Second）
- 行为检测正则必须覆盖口语表达，写完后用 10 个口语例句自测

## 三、安全铁律（零容忍）

- 禁止 `innerHTML` 直接渲染用户输入 → 用 `textContent` 或先 `escapeHTML()`
- 禁止 `alert()` / `confirm()` / `prompt()` → 用非阻塞 toast
- 禁止 `eval()` / `new Function()` / 字符串形式的 `setTimeout`
- 禁止向外部域名发送任何数据（fetch/XHR/内联事件）——所有数据只留在浏览器本地
- **Rule 8 数据隐私零容忍**：用户对话、上传材料、任何输出只用于当前任务，绝不外传
- 完整清单见 memory [[security-rules]]

## 四、协作约定

- 工作语言：中文
- 每次会话开始处理任务前，如任务涉及场景设计/方法论/安全，先读对应 memory 文件全文，不要只凭索引行工作
- 用户在对话中给出的新规则，**立即写入本文件或 memory**，不要只留在对话里（对话会被压缩，规则会丢失）
- 每周一上午有定时任务：更新 V19 方法论复盘 + 检查 memory 是否需要更新

### 5. 宏观分析报告生成规则
- 任何宏观报告任务，先读 memory [[macro-report-system]]
- 六层频率体系：日报（异常雷达）→ 周报（洞察建议）→ 月报（深度分析）→ 季报（策略重检）→ 半年报（结构审视）→ 年报（年度复盘）
- 报告必须使用 Apple HIG 设计标准，模板位于 `templates/` 目录
- 生成报告后，自动创建下一个 one-shot 定时任务以维持 cron 链不断
- 报告文件命名严格遵循：`2026宏观分析{日报/周报/月报/季报/半年报/年报}_YYYYMMDD.html`
- 报告存放于 `reports/{daily,weekly,monthly,quarterly,semiannual,annual}/`

## 五、IP 资产同步铁律（每次代码变更后）

- **本地仓库是唯一可信版本**，线上（低代码平台）是部署副本，不是代码源
### 同步策略：里程碑制，非每次改动

- **不用每次小改动都回拉**——打断工作流，效率低
- 触发回拉的时机（里程碑）：
  1. **一个功能/bug 修复完成并线上验证通过**（如"过渡页多主题方案上线、死循环修复确认"）
  2. **一次对话结束前**——把当天所有改动文件统一拉回，存一个带日期的快照
  3. **重大版本上线前**（如"TG001 真人试跑前"、"V19.1 发布前"）
  4. **Claude 产出完整新文件时**——立即存本地（用 Write 工具直接写入，不需要用户操作）
- Claude 的职责：每个里程碑到达时主动提醒，列清单
- 提醒格式：`📦 里程碑同步：以下文件今天在线上有改动，请从低代码平台编辑器复制回本地 [日期] 快照。`
- 历史教训：2026-07-16 对账发现 5 处线上/本地分叉，皆因长期未同步

### 部署前强制代码审查（2026-07-23 新增）

- **任何代码部署到低代码平台之前，必须先做完整代码审查，不得跳过。**
- 审查范围：
  1. **新增代码逐行走查** — 变量引用是否正确、异步流程是否完整、边界情况是否覆盖
  2. **新旧代码交互** — 新代码是否破坏了原有数据流（如 conversationHistory 未同步更新）
  3. **安全规则零容忍检查** — `alert()`、`eval()`、`innerHTML` 直接渲染用户输入
  4. **大括号/括号平衡** — 机械验证，防止语法错误
  5. **Console 日志确认** — 关键分支有日志输出，部署后可在 Console 验证
- 审查结果必须显式写在对话中（发现了什么、修了什么、预存问题有哪些）
- 历史教训：2026-07-23 B路线方案A部署前发现 2 个 bug（对话历史断层、死参数），如果直接部署会导致 AI 上下文丢失。同时发现 2 个预存问题（侧边栏评分条冻结、engine.history 未写入）。审查避免了"部署→发现故障→紧急修复→再次部署"的返工循环。
