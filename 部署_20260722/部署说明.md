# AI 管理陪练 · 部署说明

> 2026-07-22 · 按顺序执行

---

## Step 1：数据库迁移（两个 SQL 都要执行）

### 1a. 班级管理

执行 `班级管理_数据库迁移.sql`：
- `coaching_classes` — 班级表
- `class_members` — 班级-学员关联
- `coaching_sessions` 新增 `class_id` 外键

### 1b. 班级场景关联 🆕

执行 `班级场景关联_数据库迁移.sql`：
- `class_scenes` — 班级包含哪些场景（管理员创建班级时选择）

---

## Step 2：替换引擎页

| 本地文件 | 低代码平台位置 | 改动 |
|---------|---------|------|
| `scene_TG001_引擎页.html` | TG001 对话页 | 语音输入+手机端+数据保护+等待提示 |
| `scene_PR2_引擎驱动版_v7.html` | PR2 对话页 | 语音输入+手机端+数据保护 |

---

## Step 3：替换管理看板

| 本地文件 | 低代码平台位置 | 改动 |
|---------|---------|------|
| `管理看板.html` | 管理看板 | 班级筛选+自动总结+分层显示 |

---

## Step 4：替换首页 + 场景页（React 组件）

| 本地文件 | 替换项目中的 | 改动 |
|---------|------------|------|
| `HomePage.tsx` | `HomePage.tsx` | 嵌套 13→5 层，移动端 hamburger |
| `CoachingPage_merged.tsx` | `CoachingPage.tsx` | 三大板块+推荐+筛选+自评弹窗 |

---

## 部署验证

- [ ] 两个 SQL 执行成功
- [ ] class_scenes 表可查询
- [ ] 引擎页：语音可用、手机端正常
- [ ] 管理看板：班级下拉框可见
- [ ] 首页：移动端 hamburger 可展开
- [ ] 场景页：三大板块显示正确、筛选生效
