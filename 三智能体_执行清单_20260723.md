# 三智能体架构重构 · 执行清单

> 2026-07-23 · 综合根因分析 + 逐模块论证 + 低代码平台平台能力确认

---

## 7/18 产出对账（确保上次设计不再次落空）

| 7/18 产出 | 当前覆盖 | 对应步骤 |
|-----------|---------|---------|
| `dialogue_mode` 字段（narrative/training/drill） | ✅ 协议 v2.0 | 步骤 1 |
| `learning_theory` 字段（constructivism/behaviorism/knowledge_reinforcement） | ✅ 协议 v2.0 | 步骤 1 |
| 反馈页组件化：三种模块组可插拔 | ✅ 步骤 4 拆为三个文件 | 步骤 4 |
| 对话引擎按模式不同逻辑 | ✅ 步骤 3 facilitator 按 mode 分发 | 步骤 3 |
| 首页三大板块分组（训战/管理/业务）| ✅ 已有 CoachingPage，步骤 13 优化 | 步骤 13 |
| 业务陪练反馈（话术匹配+情绪管理+需求挖掘） | ✅ behaviorism 模块组 | 步骤 4 |
| MTP 训后反馈（知识点掌握+同班对比+老师信号） | ✅ knowledge-reinforcement 模块组 | 步骤 4 |
| 管理=建构主义 / 销售=行为主义 / MTP=知识强化 | ✅ 协议 `learning_theory` 驱动 | 步骤 1,4 |
| 两个 Tab 反馈 HTML（管理+MTP） | ✅ 功能迁移到 tsx 模块组 | 步骤 4,6 |
| ScenarioChatPage / ScenarioFeedbackPage 通用组件 | ✅ 通用化为 DialoguePage/FeedbackPage | 步骤 6 |
| 场景分类预览页（三大板块） | ✅ 已部署，步骤 13 对接新路由 | 步骤 13 |

---

## 决策记录（已确认，不再讨论）

| # | 决策 | 依据 |
|----|------|------|
| D1 | 场景卡 JSON 是唯一真相源，引擎代码不含任何场景硬编码 | 根因分析：协议18字段引擎只读5个 |
| D2 | 三种模式：管理(narrative) AI优先 / 训战(training) 回应池核心AI不参与实时对话 / 业务(drill) 回应池优先 | 120人同时开始+月底实战 |
| D3 | 反馈页：判词+觉察用AI(L3)，行为对照用规则+原话提取(L2)，其余L1 | 逐模块信息需求论证 |
| D4 | 对话降级：L1缓存 → L2 AI(3s超时) → L3 回应池 → L4 fallback（管理）；L1缓存 → L2 回应池 → L3 知识提示兜底（训战） | 低代码平台Q1确认模型分离可行 |
| D5 | v1 不做 DB 共享缓存，v2 评估 | 低代码平台Q2：有PostgreSQL可做但不急；120人场景回应池覆盖80%比DB更可靠 |
| D6 | 通用路由 `/dialogue/:sceneId` 可行 | 低代码平台Q3：React Router动态路由完全支持 |
| D7 | 管理陪练本周试跑 / 训战陪练月底实战 / 业务陪练下月 | 用户确认 |

---

## 阶段一：合约层（协议定稿 + 数据迁移）

### 步骤 1：场景卡协议 v2.0 定稿

**产出**：`场景卡数据协议_v2.0.md` + `ScenarioCard` TypeScript 接口

**内容**：
- 新增字段：`dialogue_mode`、`learning_theory`、`prompt_rules`（含 traps/whispers/context_vars/auto_advance）、`success_criteria`（含 behavior_steps）、`feedback_modules`
- 每个字段注明：谁来读（对话引擎/引导者/反馈引擎）、什么时候读、读不到时的默认值
- 可选字段：`knowledge_points`（训战特有）、`script_steps`（业务特有）

**验证**：协议文件 + TS 接口无遗漏字段，对照 v1.0 协议做 diff 确认新增项

**预估**：半天

---

### 步骤 2：TG001 场景卡按 v2.0 补全

**产出**：`TG-001_v2.0.json`

**内容**：从当前引擎代码中提取硬编码值填入 JSON：
- `system_prompt` ← `getTG001SystemPrompt()`
- `response_pool` ← `tg001-response-pool.ts`（已有，核对协议格式）
- `prompt_rules.traps` ← `detectTraps()` 中的 6 条正则规则
- `prompt_rules.whispers` ← `showWhisper()` 触发逻辑
- `prompt_rules.context_vars` ← `updateContext()` 中的正则
- `prompt_rules.auto_advance` ← 防卡死自动推进规则
- `success_criteria` ← 引擎中的 4 个维度 + `getBehaviorSteps` 的 4 步定义
- `dialogue_mode` = `"narrative"`
- `learning_theory` = `"constructivism"`
- `feedback_modules` = `["verdict", "awareness", "behavior_steps", "next_practice", "knowledge_gaps", "action_plan"]`

**验证**：脚本遍历 JSON 所有字段，确认引擎接口能消费，无 `undefined`

**预估**：半天

---

## 阶段二：引擎层（对话 + 反馈重构）

### 步骤 3：对话引擎重构为配置驱动

**产出**：`dialogue-agent/engine.ts` + `dialogue-agent/facilitator.ts`

**内容**：
- `respond(sceneConfig, userMsg, history)` — 唯一入口，读场景卡 JSON
  - narrative 模式：L1 缓存 → L2 AI(3s超时) → L3 `sceneConfig.response_pool` → L4 fallback
  - training 模式：L1 缓存 → L2 `sceneConfig.response_pool`(增强版，5-6变体/MTP知识点匹配) → L3 知识提示兜底
- `getFacilitatorHints(sceneConfig, userMsg, context, round)` — 按 `dialogue_mode` 分发
  - narrative → 轻声提醒
  - training → 知识提示（读 `sceneConfig.knowledge_points`）
- `detectEvents(sceneConfig, userMsg, context)` — 按 `dialogue_mode` 分发
  - narrative → `sceneConfig.prompt_rules.traps` + NPC 状态推进
  - training → `sceneConfig.knowledge_points` 知识点遗漏检测 + 决策点插入

**输入**：TG001EnginePage.tsx(528行) + tg001-b-route.ts(131行) + tg001-engine-logic.ts(上下文+陷阱)

**当前 PR2 回应池缺失**：PR2 是独立 HTML，没有回应池。重构后 PR2 也可以走通用引擎——但需要为 PR2 单独补一个回应池 JSON。本次 TG001 先行，PR2 在步骤 9 迁移。

**验证**：A/B 对比——同样的对话输入，新旧引擎输出一致（响应文本可以不同，但 NPC 状态推进、陷阱触发、分数变化一致）

**预估**：1.5 天

---

### 步骤 4：反馈引擎重构为配置驱动 + 模块化

**产出**：`feedback-agent/generator.ts` + `feedback-agent/modules/constructivism.ts` + `feedback-agent/modules/knowledge-reinforcement.ts` + `feedback-agent/modules/behaviorism.ts`

**内容**：
- `generate(sceneConfig, sessionData)` → 返回 `FeedbackModule[]`
- 按 `sceneConfig.learning_theory` 加载对应模块组
- constructivism 模块组（管理陪练）：判词(L2模板+原话变量) + 觉察(L2模板+原话变量) + 行为对照(规则+原话提取) + 再来一练 + 知识薄弱点 + 下一步计划
  - **判词和觉察先做 L2 版本，步骤 5 再接入 AI**
- knowledge-reinforcement 模块组（训战陪练）：知识点 Checklist + 薄弱点明细 + 老师信号 + 再来一练
  - behaviorism 模块组（业务陪练）：话术步骤完成率 + 遗漏元素标注 + 客户情绪管理评分 + 需求挖掘深度 + 下次强化方向
- 每个模块文本模板嵌入 `{user_quote}` / `{npc_reaction}` 变量，从 `sessionData.history` 动态填充

**输入**：tg001-feedback-logic.ts(335行)

**验证**：TG001 场景卡 v2.0 驱动，反馈输出引用对话原话（验证至少 2 处 `{user_quote}` 被真实用户发言替换）

**预估**：1 天

---

### 步骤 5：反馈 AI 插件接入（判词 + 觉察）

**产出**：反馈模块中判词和觉察升级为 AI 生成

**内容**：
- 调用 `ai_coach_analysis_report_1` 插件
- 传入完整 `history`（对话记录）+ `scores` + `traps`
- 用 AI 生成判词和觉察模块的文本
- AI 失败 → 降级到步骤 4 的 L2 模板版

**验证**：
- AI 成功 → 判词/觉察文本引用了对话中的具体事件（不仅仅是数据变量）
- AI 失败 → L2 降级正常展示
- Console 日志确认调用和降级路径

**预估**：半天

---

## 阶段三：页面层（通用化）

### 步骤 6：对话页/反馈页通用化

**产出**：`DialoguePage.tsx` + `FeedbackPage.tsx`（通用）

**内容**：
- `/dialogue/:sceneId` → 加载 `sceneConfig` → 渲染对话页
- `/feedback/:sceneId/:sessionId?` → 加载 `sceneConfig` + `sessionData` → 渲染反馈页
- 根据 `sceneConfig.dialogue_mode` 切换侧边栏和引导者面板
- 根据 `sceneConfig.feedback_modules` 渲染对应反馈模块组

**输入**：TG001EnginePage.tsx(528行) + TG001FeedbackPage.tsx(276行)

**验证**：`/dialogue/TG-001` 正常加载和对话，`/feedback/TG-001` 正常展示反馈

**预估**：半天

---

## 阶段四：验证与试跑

### 步骤 7：TG001 端到端回归测试

**验证清单**：
- [ ] 准备页 → 正常加载
- [ ] 对话页 → 发 3 条消息 → Console 日志确认降级层触发（L1/L2/L3/L4 各有命中）
- [ ] 对话页 → AI 优先策略生效（管理陪练模式）
- [ ] 对话页 → 连续发两个不同问题 → NPC-周回复不同（上下文 Bug ✅ 已确认修复）
- [ ] 结束对话 → localStorage 数据写入 → 反馈页跳转
- [ ] 反馈页 → 6 个模块正常展示 → 判词/觉察引用了对话原话
- [ ] 反馈页 → 行为对照的 npcReaction 和 whyExplanation 来自真实对话而非模板
- [ ] 练习记录 → localStorage `mdp_sessions` 有本次记录
- [ ] 再练一次 → 跳转回准备页
- [ ] **手机端真机测试**：对话页输入框不遮挡、回复正常、反馈页可滚动（3位管理者可能用手机打开）
- [ ] **CourseDetailPage 路由验证**：`/course/:courseId` 跳转正常、UniversalLink 生效
- [ ] **上下文Bug 正式关闭**：自测时已确认修复，在回归测试中二次确认 → 更新 memory

**预估**：半天

---

### 步骤 8：真人试跑

- 3 位真实管理者
- 完整流程：准备页 → 对话 → 反馈页
- 测评表：NPS / 五原则 / NPC 真实感 / 反馈页有用度
- 目标：NPS ≥ 40 / 反馈页被评为"跟我的对话有关"

**预估**：本周

---

## 阶段五：扩展（训战 + 业务，试跑后执行）

### 步骤 9：TG001 训战模式回应池增强

**产出**：`TG-001_training_response_pool.json`

**内容**：
- 从当前 54 条扩展到 100+ 条（每个 intent 5-6 变体）
- 新增 MTP 知识点关联（每条回应标注对应 MTP 知识点 ID）
- 新增引导者提示变体池

**预估**：1 天

---

### 步骤 10：PR2 迁移到新架构

**内容**：
- 按 v2.0 协议补全 PR2 场景卡 JSON
- 补全 PR2 回应池（管理陪练模式）
- PR2 准备页从独立 HTML 迁移到通用准备页
- 验证 PR2 在新架构下正常运行

**预估**：1 天

---

### 步骤 11：TG002/TG003 场景卡补全 + 引擎页生成

**内容**：
- TG002/TG003 按 v2.0 协议补全场景卡 JSON
- 补全回应池
- 通过通用对话页直接可用（不需要写任何新代码）

**预估**：每个场景半天

---

### 步骤 12：训战陪练管理看板

**内容**：
- 老师端：实时查看 120 人薄弱点分布
- 基于 `sessionData` 汇总知识点掌握情况
- 标注"下节课需要强化的要点"

**预估**：1 天

---

### 步骤 13：首页三大板块对接（7/18 #17 落地）

**产出**：CoachingPage 优化 + 三大板块路由对接

**内容**：
- 训战陪练板块 → `/dialogue/:sceneId`（通用对话页）
- 管理陪练板块 → 场景卡片保持现有筛选逻辑，跳转统一为 `/dialogue/:sceneId`
- 业务陪练板块 → 空状态引导 + 待建标记
- 场景分类预览页对接新路由

**验证**：首页任意场景卡片点击 → 正确加载对话页，场景卡 JSON 驱动

**预估**：半天

---

### 步骤 14：场景生成智能体 Prompt 初稿

**产出**：场景生成智能体 Prompt 文档 + 使用说明

**为什么做**：next-tasks.md 列为 P0。三智能体插件已封装（`scenario_card_smart_generate_v19_1`），D-P-f 映射规则已就绪，案例库 JSON 已有 4 个场景。但「如何使用插件生成新场景」的完整 Prompt 模板和使用说明从未输出。月底训战扩展时需要生成新场景——不能每次都手写 JSON。

**内容**：
- 场景生成 Prompt 模板（含 D-P-f 映射规则嵌入）
- 使用说明：输入什么 → 输出什么 → 如何验证输出质量
- 与场景卡协议 v2.0 对齐（生成的 JSON 直接符合 v2.0 协议）

**验证**：用 Prompt 生成一个新场景的 JSON，对照 v2.0 协议验证字段完整性

**预估**：半天

---

### 步骤 15：三智能体规则文档

**产出**：三智能体规则文档（场景生成规则 + 对话规则 + 反馈规则）

**为什么做**：project-sandbox-architecture.md 明确列为未输出项。三个智能体的完整规则模板——不是代码协议，是面向使用者的说明：每个智能体做什么、输入什么、输出什么、有什么约束。重构完成后写——代码落地了，文档才有依据。

**内容**：
- 场景生成智能体规则：如何从案例描述生成场景卡 JSON（D-P-f 映射 + 协议字段说明）
- 对话智能体规则：三种模式的引擎行为、System Prompt 构建规则、引导者介入规则
- 反馈智能体规则：三种学习理论的反馈框架、评分维度来源、AI 模块调用规则

**预估**：1 天

---

## 独立跟踪项（不纳入架构重构，但需登记）

| # | 事项 | 优先级 | 来源 | 说明 |
|----|------|--------|------|------|
| T1 | 工程通用胜任力访谈回收跟进 | P2 | next-tasks #P1 | 模板已发出，需主动跟进。D 向量验证依赖此数据 |
| T2 | 场景卡片配图替换 | P2 | 每日总结 7/23 | 当前用占位图，不影响功能 |
| T3 | 方法论授课版修订 | P2 | next-tasks #P2 | 依赖真人试跑结果 |

---

## 汇总

| 阶段 | 步骤 | 预估 | 依赖 |
|------|------|------|------|
| 合约 | 1. 协议 v2.0 | 半天 | — |
| 合约 | 2. TG001 v2.0 JSON | 半天 | 步骤1 |
| 引擎 | 3. 对话引擎重构 | 1.5天 | 步骤2 |
| 引擎 | 4. 反馈引擎重构 | 1天 | 步骤2 |
| 引擎 | 5. 反馈AI接入 | 半天 | 步骤4 |
| 页面 | 6. 通用对话页/反馈页 | 半天 | 步骤3,4 |
| 验证 | 7. 回归测试 | 半天 | 步骤5,6 |
| 试跑 | 8. 真人试跑 | 本周 | 步骤7 |
| 扩展 | 9. 训战回应池增强 | 1天 | 步骤8 |
| 扩展 | 10. PR2迁移 | 1天 | 步骤8 |
| 扩展 | 11. TG002/003补全 | 1天 | 步骤10 |
| 扩展 | 12. 训战管理看板 | 1天 | 步骤9 |
| 扩展 | 13. 首页三大板块对接 | 半天 | 步骤6 |
| 扩展 | 14. 场景生成智能体 Prompt 初稿 | 半天 | 步骤1 |
| 扩展 | 15. 三智能体规则文档 | 1天 | 步骤8 |

**阶段一至四（管理陪练试跑）：约 4.5 天**
**阶段五（训战 + 业务 + PR2 + 首页 + Prompt + 规则文档）：约 6 天**
**总计：约 10.5 天**
