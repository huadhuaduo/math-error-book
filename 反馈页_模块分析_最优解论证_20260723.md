# 反馈页模块深度分析：规则 vs AI 最优解论证

> 2026-07-23 · 基于 tg001-feedback-logic.ts 代码逐模块分析

## 分析框架

每个模块从三个维度论证最优解：
- **信息需求**：这个模块要回答什么问题？需要什么粒度的数据？
- **数据可用性**：`sessionData` 里有没有这些数据？如果有，规则引擎能做到什么程度？
- **失败模式**：如果方案失败，用户看到的是什么？降级方案够不够好？

---

## 模块 1：判词（getVerdict）

### 它要回答什么
"我这次做得怎么样？最核心的一个判断是什么？"

### 当前代码的数据输入
`getVerdict(session)` 只读了三个字段：`npcState`、`phase`、`revealed`。4条预写判词，按以下逻辑分发：
- FOLLOWING + DEMO → 判词A："面对团队不信时，你替他们做了减法"
- directionGiven + !demoDone → 判词B："你给了更多工具，而不是替他们做减法"
- trustAsked + !directionGiven → 判词C："你在等他替你决定"
- 其他 → 判词D："你太快进入解决问题模式"

### 规则引擎能做到什么程度
理论上，可以把所有 `(npcState × phase × traps × revealed × scores)` 的组合枚举出来，每个组合写一条判词。

组合爆炸：4个npcState × 4个phase × 6个traps(各有/无) × 3个revealed(各T/F) × 4个scores(各三档) = 4 × 4 × 2^6 × 2^3 × 3^4 ≈ 4 × 4 × 64 × 8 × 81 ≈ 663,552 种组合。

实际上很多组合不可能同时出现，但即使压缩到100条规则，每条判词要写出差异——几乎不可能。而且判词不能引用用户原话——因为它只读了 `revealed`(bool)，不知道用户说了什么。

**规则引擎的天花板：能判断"结果好坏"，不能解释"为什么是这个结果"。**

### AI 能做到什么
给 AI 传入完整 `history`（对话记录）：
- "在第2轮，你问了NPC-周'你觉得哪两个户型值得主力推'——这是他今天第一次正眼看你。你跳过了一上来就发号施令的本能。"
- "但在第4轮，当NPC-周说'下面的人不信'时，你没有亲自打样——你给了更多分析工具。NPC-周表面上接受了，但他的状态停在了'验证'——没有进入'跟从'。"

这就是因果推理——AI 的强项。不是"你做得不好"，是"你在这一步做了X，导致了Y的结果"。

### 降级分析
如果 AI 调用失败，降级方案是什么？

降级到L2：模板 + 数据变量 + 原话提取。
```
判词模板：你在{npcState}状态下结束了对话。你做到了{passedChecks}，但{missedChecks}。
关键转折点：当你说"{extractKeyQuote(history, 'user', 'longest')}"时，NPC-周的反应是"{extractKeyQuote(history, 'npc', 'after_user_longest')}"。
```
这比不上AI的因果分析，但远比当前的纯模板好——因为它引用了用户说的话。

### 论证结论
**判词用AI是最优解。** 因果推理是规则的天花板，是AI的强项。降级方案(L2)比当前好10倍，AI版本比降级方案好3倍。每次练习只需调用1次AI。


## 模块 2：觉察·你的模式（getManagementInertia）

### 它要回答什么
"你在压力下的默认反应是什么？这个模式从哪来的？它对你的管理效果有什么影响？"

### 当前代码的数据输入
`getManagementInertia(session)` 读 `traps` + `revealed`。4条预写模式：
- 施压型（T09触发）→ "你默认用压力和问责推动执行"
- 信息工具型（T03或T04触发）→ "你默认给更多信息和工具"
- 先方案后提问型（!trustAsked）→ "你跳过了问'你怎么看'"
- 做加法型（兜底）→ "你在压力下倾向于做加法"

### 规则引擎能做到什么程度
比判词更差。判词至少有"结果好坏"这个客观锚点。模式识别需要：
- 识别**重复出现**的行为倾向（不只一次触发，而是多次）
- 推断**心理根源**（为什么这么做？不安全感？效率导向？对资深员工的不信任？）
- 连接**管理后果**（这个模式对团队意味着什么？）

这三层都需要自然语言理解。用户可以触发T03一次（"给了对比表"），也可以触发T03三次（"给了对比表、给了数据报告、给了培训资料"）——前者是一次失误，后者是模式。规则引擎只知道"T03>=1"，无法区分"一次"和"三次"。

**规则引擎的天花板：能检测"触发过什么陷阱"，不能识别"行为模式"。**

### AI 能做到什么
给 AI 传入完整 `history`：
- "在对话中，你三次面对NPC-周的'不信'信号时，都选择了'给更多信息'这个策略。第一次你给了对比表，第二次你给了客户数据报告，第三次你提议培训话术。每次都避开了'亲自做给他们看'这个选项。这不是能力问题——这是默认策略问题。面对资深团队的不信任，你的本能是'用更多理据说服'而非'用行动示范'。"

这才是"觉察"——让学员看到自己没看到的模式。

### 论证结论
**觉察用AI是最优解。** 模式识别是规则无法做到的——规则只能计次，不能识别模式。降级方案(L2)用模板+"你触发了{trap_list}陷阱"代替，远不如AI但比当前好。


## 模块 3：行为对照（getBehaviorSteps）

### 它要回答什么
"每一步：你做了什么？绩优做法是什么？差距在哪？"

### 当前代码的数据输入
`getBehaviorSteps(session)` 读 `revealed` + `traps` + `phase`。4步，每步good/bad/skip三种状态，每个状态有预写的npcReaction和whyExplanation。共约36条预写文本。

### 分析：这真的是"个性化"需求吗？
不是。行为对照的**判断逻辑**（good/bad/skip）是确定的——来自场景卡的成功标准。TG001的四步是固定的：①先做事不说话 ②先问后说 ③替团队做减法 ④亲自打样。每步的判断标准是`revealed`和`traps`中的对应字段。

需要的个性化不是"判断逻辑"的个性化——是**上下文**的个性化：
- npcReaction应该是真实的，不是预写的："NPC-周放下保温杯，语气变了" — 这在对话记录里有
- whyExplanation应该关联用户说了什么："你问了他'你觉得哪两个值得主力推'——这句话同时做了三件事..."

### L2方案：规则判断 + 原话提取
```
对于每一步：
  判断(good/bad/skip) ← 规则引擎(已有: revealed, traps)
  
  如果good:
    npcReaction ← 从history提取这一步对应的NPC回应
    whyExplanation ← 模板 + {user_quote} + {npc_reaction}
  
  如果bad:
    whyExplanation ← 模板 + {user_quote} + {绩优做法} + {差异分析}
```

这需要从history中提取对应用户发言——这是字符串处理，不是AI。

### 论证结论
**行为对照用L2(规则+原话提取)是最优解。** 判断逻辑规则就能做（本就应该从场景卡读取，不是从代码硬编码），个性化来自"把用户实际说的话嵌入分析模板"。AI没有额外价值——这一步需要的不是因果推理，是"把正确的原话放在正确的位置"。


## 模块 4-6：再来一练 + 知识薄弱点 + 下一步计划

### 分析
三个模块的共同特征：**数据驱动的推荐/分类，不需要自然语言生成。**

- 再来一练：基于最弱维度推荐重练方向 → `min(scores)`就确定了方向
- 知识薄弱点：基于scores分档(need/consolidate/mastered) → 阈值判断
- 下一步计划：用户输入 → 系统只提供框架

### 论证结论
**L1就够了。** 这三个模块的价值在于"数据准确"和"UI清晰"，不在于"文本优美"。


---

## 最终结论

| 模块 | 方案 | AI调用 | 降级方案 |
|------|------|--------|---------|
| 判词 | AI生成（L3） | 1次/练习 | 模板+数据变量+原话提取(L2) |
| 觉察·模式 | AI生成（L3） | 1次/练习 | 模板+"你触发了{trap_list}"(L2) |
| 行为对照 | 规则+原话提取(L2) | 0 | — |
| 再来一练 | 规则引擎(L1) | 0 | — |
| 知识薄弱点 | 规则引擎(L1) | 0 | — |
| 下一步计划 | 用户输入(L1) | 0 | — |

**每次练习AI调用量：2次。对话AI的1/5到1/7。** 3人同时看反馈=6次AI调用，远低于并发测试中50次80%成功率的安全线。

**不搞调和——每个模块的最优方案是独立的，有明确的论证链条。**
