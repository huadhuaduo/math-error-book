# 三智能体架构重构 · 终审执行清单

> 2026-07-23 · 专家团终审 · 按优先级排列
> 核心约束：管理本周试跑 / 训战月底 120 人实战 / 业务下月

---

## 🔴 P0：本周必须完成（管理陪练真人试跑阻断项）

### 步骤 1：场景卡协议 v2.0 定稿

**解决什么问题**：引擎代码不知道当前是什么场景类型。dialogue_mode、learning_theory、prompt_rules 三个关键字段 7/18 设计后从未进入协议，导致引擎无法按模式分发。

**为什么是 P0**：所有后续步骤的前置依赖。协议不定，引擎重构没有依据。

**产出**：`场景卡数据协议_v2.0.md` + `ScenarioCard` TypeScript 接口

**新增字段**：`dialogue_mode`（narrative/training/drill）、`learning_theory`（constructivism/knowledge_reinforcement/behaviorism）、`prompt_rules`（traps/whispers/context_vars/auto_advance）、`success_criteria`（含 behavior_steps）、`feedback_modules`。`knowledge_points` 和 `script_steps` 作为可选字段进入协议，引擎预留接口。

**验证**：每个字段注明"谁来读、什么时候读、读不到时的默认值"

**预估**：半天

---

### 步骤 2：TG001 场景卡按 v2.0 补全

**解决什么问题**：引擎硬编码了 `getTG001SystemPrompt()`、`detectTraps()`、`updateContext()`——内容更新必须改代码。全部抽到 JSON。

**为什么是 P0**：对话引擎重构的前置——引擎需要一份完整的 v2.0 JSON 来验证协议字段是否够用。

**产出**：`TG-001_v2.0.json`

**内容**：从当前代码提取硬编码值：
- `system_prompt` ← `getTG001SystemPrompt()`
- `response_pool` ← `tg001-response-pool.ts`
- `prompt_rules.traps` ← `detectTraps()` 6 条规则
- `prompt_rules.whispers` ← `showWhisper()` 触发逻辑
- `prompt_rules.context_vars` ← `updateContext()` 正则
- `prompt_rules.auto_advance` ← 防卡死规则
- `success_criteria` ← 4 维度 + 4 步行为对照定义
- `dialogue_mode` = `"narrative"`、`learning_theory` = `"constructivism"`、`feedback_modules` = 管理陪练 6 模块组

**验证**：脚本遍历 JSON，确认引擎接口能消费，无 `undefined`

**预估**：半天

---

### 步骤 3：对话引擎统一入口

**解决什么问题**：TG001 有一套 tsx 引擎，PR2 有一套 HTML 引擎——修复一个不影响另一个，120 人场景修 bug 不能逐个文件改。

**为什么是 P0**：真人试跑后修的每个 bug 都需要复用到训战模式。入口不统一 = 修两次。

**产出**：`dialogue-agent/engine.ts` + `dialogue-agent/facilitator.ts`

**内容**：
- `respond(sceneConfig, userMsg, history)` — 所有场景共用
  - narrative 模式（管理）：L1 缓存 → L2 AI(3s超时) → L3 `sceneConfig.response_pool` → L4 fallback
  - training 模式（训战）：L1 缓存 → L2 `sceneConfig.response_pool` → L3 知识提示兜底（AI 不参与实时对话）
- `getFacilitatorHints()` 和 `detectEvents()` 按 `dialogue_mode` 分发，读 `sceneConfig.prompt_rules`
- training 和 drill 模式的方法签名预留，逻辑月底补

**输入**：TG001EnginePage.tsx(528行) + tg001-b-route.ts(131行) + tg001-engine-logic.ts

**验证**：A/B 对比——同样输入，新旧引擎 NPC 状态推进、陷阱触发、分数变化一致

**预估**：1.5 天

---

### 步骤 4：反馈引擎重构（L2 原话锚定 + constructivism 模块组）

**解决什么问题**：反馈页 6 个模块全部是预写模板——不引用学员说的任何一句话。`getVerdict` 有 4 条模板，`getBehaviorSteps` 有 36 条模板。学员看完觉得"这不是在说我"。

**为什么是 P0**：反馈页让学员觉得"跟我的对话无关"是试跑阻断项——NPS 和反馈页有用度评分直接挂。

**为什么只做 L2、不做 AI**：L2（模板 + 从 history 动态提取原话嵌入 `{user_quote}` 变量）解决根因——"不引用学员原话"。AI 解决的是"洞察不够深"——这是增强，不是阻断。真人试跑先验证 L2 够不够，不够再加 AI。

**产出**：`feedback-agent/generator.ts` + `feedback-agent/modules/constructivism.ts`

**内容**：
- `generate(sceneConfig, sessionData)` → 读 `sceneConfig.feedback_modules` → 加载对应模块组
- constructivism 模块组：判词(L2模板+原话变量) + 觉察(L2模板+原话变量) + 行为对照(规则判断+原话提取) + 再来一练(L1) + 知识薄弱点(L1) + 下一步计划(用户输入)
- 每个文本模板嵌入 `{user_quote}` / `{npc_reaction}` 变量，从 `sessionData.history` 动态填充
- knowledge-reinforcement 和 behaviorism 模块组文件创建但暂不实现，引擎预留接口

**输入**：tg001-feedback-logic.ts(335行)

**验证**：反馈页至少 2 处 `{user_quote}` 被真实用户发言替换；行为对照的 npcReaction 来自对话记录而非预写

**预估**：1 天

---

### 步骤 5：TG001 端到端回归测试

**解决什么问题**：每次部署靠"感觉"判断有没有 bug——上次 `conversationHistory` 未更新的 bug 就是这样漏过去的。

**为什么是 P0**：试跑前最后一道防线。再让真人测出基础 bug，信用就没了。

**验证清单**：
- [ ] 准备页正常加载
- [ ] 对话页：发 3 条消息，Console 确认降级层触发（L1/L2/L3/L4 各有命中）
- [ ] 对话页：AI 优先策略生效（管理陪练 narrative 模式）
- [ ] 对话页：连续发两个不同问题 → NPC-周回复不同 → **上下文 Bug ✅ 正式关闭**
- [ ] 结束对话 → localStorage 数据写入（tg001_scores/traps/last_session 非 null）
- [ ] 反馈页：6 个模块正常展示，判词/觉察引用对话原话
- [ ] 反馈页：行为对照的 npcReaction 来自真实对话
- [ ] 练习记录：localStorage `mdp_sessions` 有本次记录
- [ ] 再练一次 → 跳转回准备页
- [ ] **手机端真机测试**：输入框不遮挡、回复正常、反馈页可滚动
- [ ] **CourseDetailPage 路由验证**：`/course/:courseId` 跳转正常

**预估**：半天

---

### 步骤 6：真人试跑

**内容**：3 位真实管理者，完整流程：准备页→对话→反馈页

**测评**：NPS ≥ 40 / 反馈页"跟我的对话有关" / NPC 真实感 ≥ 4.0 / 五原则均分 ≥ 7.0

**预估**：本周

---

## 🟡 P1：月底前完成（训战陪练 120 人实战阻断项）

### 步骤 7：训战模式回应池增强

**解决什么问题**：120 人同时练习，当前 54 条回应池每(intent, mood)约 1.5 条变体。同班同学互相能交流，同一句回应被 20+ 人看到——重复感不可接受。

**为什么是 P1**：管理试跑不需要。训战月底实战必须。

**产出**：`TG-001_training_response_pool.json`

**内容**：54 条 → 100+ 条（每个 intent 5-6 变体），每条关联 MTP 知识点 ID，新增引导者提示变体池。回应池语言审核同步完成（地产行业专家要求）。

**预估**：1 天

---

### 步骤 8：引导者模块独立

**解决什么问题**：训战模式需要"知识提示"（MTP3·8：非权力影响力来自专家权+典范权），管理模式的"轻声提醒"（她看起来快哭了）替代不了。当前引导逻辑散落在引擎代码里。

**为什么是 P1**：管理试跑沿用现有 whisper 逻辑即可。训战上线前必须独立。

**产出**：`facilitator.ts` 完整实现

**内容**：training 模式知识提示逻辑（读 `sceneConfig.knowledge_points`，检测学员是否应用了知识点，未应用时提示）

**预估**：半天

---

### 步骤 9：knowledge-reinforcement 反馈模块组

**解决什么问题**：训战反馈需要"知识点 Checklist + 薄弱点明细 + 老师信号 + 全班对比"，不是当前的行为对照框架。

**为什么是 P1**：训战实战必须。

**产出**：`feedback-agent/modules/knowledge-reinforcement.ts`

**预估**：1 天

---

### 步骤 10：训战管理看板

**解决什么问题**：老师下课后需要立即看到 120 人薄弱点分布，决定下节课强化哪个要点。

**为什么是 P1**：管理试跑不需要看板。训战实战必须有——这是老师"统一喊开始→看薄弱点→针对性反馈"闭环的关键。

**预估**：1 天

---

### 步骤 11：PR2 迁移到新架构

**解决什么问题**：PR2（管理者B·绩效面谈）目前是独立 HTML，无回应池，和 TG001 是两套代码。重构后统一走新引擎。

**为什么是 P1**：下周管理试跑只需要 TG001。但 PR2 是下一个管理场景，越早迁移越好。

**内容**：按 v2.0 协议补全 PR2 场景卡 JSON + 补全回应池（管理陪练模式）+ 准备页迁移

**预估**：1 天

---

### 步骤 12：对话页/反馈页通用化

**解决什么问题**：TG001 一个页面组件、PR2 一个页面组件、TG002/003 还没有页面——每次改 UI 要改 N 个文件。

**为什么是 P1 不是 P0**：管理试跑用 TG001 独立页面即可。通用化降低后续场景的边际成本。

**产出**：`DialoguePage.tsx` + `FeedbackPage.tsx`（读场景卡 JSON 渲染）

**预估**：半天

---

## 🟢 P2：下月完成（业务陪练 + 收尾）

### 步骤 13：反馈 AI 接入（判词 + 觉察）

**解决什么问题**：L2 让反馈"跟对话有关"，AI 让它"有洞察深度"——因果推理和模式识别是规则的天花板。

**为什么是 P2 不是 P0**：L2 解决了试跑阻断项（反馈不引用原话）。AI 是增强——试跑数据会告诉我们 L2 够不够。

**验证**：AI 成功→判词引用对话具体事件；AI 失败→降级到 L2 模板（步骤 4 已实现）

**预估**：半天

---

### 步骤 14：behaviorism 反馈模块组

**解决什么问题**：业务陪练需要话术步骤完成率 + 遗漏元素标注 + 客户情绪管理评分。

**为什么是 P2**：业务陪练下月才上线。

**预估**：1 天

---

### 步骤 15：TG002/TG003 场景卡补全

**解决什么问题**：两个场景只有 JSON 骨架，无回应池，无完整 prompt_rules。通用引擎就绪后补全。

**为什么是 P2**：不阻塞管理和训战。但通用化之后边际成本极低——每个场景半天。

**预估**：每个场景半天

---

### 步骤 16：首页三大板块对接（7/18 #17 落地）

**内容**：三大板块路由统一对接通用对话页，场景分类预览页对接新路由

**预估**：半天

---

### 步骤 17：场景生成智能体 Prompt 初稿

**解决什么问题**：next-tasks.md 列 P0。三智能体插件已封装，D-P-f 已就绪，但"如何使用插件生成新场景"的完整 Prompt 模板从未输出。

**为什么是 P2 不是 P0**：TG001 已存在，TG002/003 JSON 已存在。现在不需要生成新场景。月底训战扩展时按需使用。

**预估**：半天

---

### 步骤 18：三智能体规则文档

**解决什么问题**：project-sandbox-architecture 明确列为未输出项。场景生成+对话+反馈三个智能体的完整规则模板。

**为什么是 P2**：重构完成后再写——代码落地了，文档才有依据。

**预估**：1 天

---

## 独立跟踪项（不纳入架构重构）

| # | 事项 | 优先级 | 说明 |
|----|------|--------|------|
| T1 | 工程通用胜任力访谈回收跟进 | P2 | 模板已发出，需主动跟进。D 向量验证依赖此数据 |
| T2 | 场景卡片配图替换 | P2 | 当前用占位图，不影响功能 |
| T3 | 方法论授课版修订 | P2 | 依赖真人试跑结果 |

---

## 汇总

| 优先级 | 步骤 | 天数 | 死线 |
|--------|------|------|------|
| 🔴 P0 | 1. 协议 v2.0 → 2. TG001 JSON → 3. 对话引擎 → 4. 反馈 L2 → 5. 测试 → 6. 试跑 | 4.5 | **本周** |
| 🟡 P1 | 7. 回应池增强 → 8. 引导者 → 9. 训战反馈 → 10. 看板 → 11. PR2 → 12. 通用化 | 5 | **月底** |
| 🟢 P2 | 13. 反馈AI → 14. behaviorism → 15. TG002/003 → 16. 首页 → 17. Prompt → 18. 规则文档 | 4 | **下月** |
| 📋 跟踪 | T1. 访谈 T2. 配图 T3. 授课版 | — | 持续 |

**总工作量：约 13.5 天（含独立跟踪项）**
**P0 关键路径：步骤 1→2→3+4→5→6，4.5 天**
