# TG001 回归测试清单

> 部署后逐项验证，每项打勾。不靠感觉——每项有明确的通过标准。

---

## 一、协议层验证

- [ ] **1.1 TS接口编译通过**：`场景卡数据协议_v2.0.ts` 无 TypeScript 编译错误
- [ ] **1.2 JSON格式合法**：`TG-001_v2.0.json` 可被 `JSON.parse()` 解析
- [ ] **1.3 必填字段全覆盖**：脚本遍历JSON，12个必填字段非null/非undefined
- [ ] **1.4 无 eval() 依赖**：grep 所有文件，无 `eval(` 调用（注释除外）

## 二、对话引擎

- [ ] **2.1 引擎初始化**：`createInitialState()` 返回正确的初始状态
- [ ] **2.2 上下文更新**：输入"我想看看这个户型的数据" → `ctx.bizMentioned` 变为 `true`
- [ ] **2.3 陷阱检测**：输入"明天全员动员会，宣布新策略" → 触发 T01-先发制人
- [ ] **2.4 条件求值安全**：输入触发T01的文本，但 ctx.bizMentioned 已为 true → 不触发T01
- [ ] **2.5 AI调用优先**：管理陪练模式下，Console日志确认L2(AI)优先于L3(回应池)
- [ ] **2.6 回应池降级**：AI超时/失败时，降级到回应池（Console: L3）
- [ ] **2.7 Fallback兜底**：回应池也无匹配时，返回通用 fallback（Console: L4）
- [ ] **2.8 上下文Bug确认关闭**：连续发两个不同问题 → 老周回复不同
- [ ] **2.9 对话历史完整性**：每轮对话后，history 包含 user + assistant 各一条

## 三、反馈引擎

- [ ] **3.1 判词引用原话**：判词模块 content 包含 `{user_quote}` 被替换后的真实用户发言
- [ ] **3.2 觉察引用原话**：觉察模块 content 包含真实用户发言
- [ ] **3.3 行为对照状态正确**：good/bad/skip 判断基于 ctx 实际值
- [ ] **3.4 行为对照引用NPC反应**：npc_reaction 来自对话记录
- [ ] **3.5 再来一练指向最弱维度**：推荐方向 = scores 中的最低分维度
- [ ] **3.6 知识薄弱点列表完整**：success_criteria 的每个维度都有条目

## 四、端到端

- [ ] **4.1 准备页→对话页**：正常加载，NPC开场白正确
- [ ] **4.2 完整对话流程**：≥8轮对话，NPC状态从WATCH推进到FOLLOW
- [ ] **4.3 结束对话→反馈页**：localStorage 数据写入 → 跳转正常
- [ ] **4.4 反馈页6模块展示**：判词/觉察/行为对照/再来一练/薄弱点/行动计划
- [ ] **4.5 练习记录保存**：`mdp_sessions` 有本次记录
- [ ] **4.6 再练一次**：反馈页点击 → 回到准备页

## 五、兼容性

- [ ] **5.1 手机端**：对话页输入框不遮挡、回复正常、反馈页可滚动
- [ ] **5.2 语音输入**：点击麦克风 → 识别 → 填入输入框
- [ ] **5.3 CourseDetailPage路由**：`/course/:courseId` 跳转正常

## 六、安全

- [ ] **6.1 无 alert()**：整个对话+反馈流程无弹窗
- [ ] **6.2 无 innerHTML 直接渲染用户输入**：用户消息经过 escHtml
- [ ] **6.3 localStorage写入验证**：tg001_scores/traps/last_session 非null

---

## 通过标准

- 一~三（协议+引擎）：12/12 项通过
- 四（端到端）：6/6 项通过
- 五（兼容性）：3/3 项通过
- 六（安全）：3/3 项通过

**全部 24 项通过 → 可以真人试跑。**
