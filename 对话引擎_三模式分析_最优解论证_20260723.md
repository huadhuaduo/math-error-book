# 对话引擎深度分析：三模式架构最优解论证

> 2026-07-23 · 基于 TG001EnginePage.tsx / tg001-b-route.ts / tg001-response-pool.ts 代码分析

## 核心问题

"回应不精准"有三个不同层面的根因，必须分开分析：

---

## 分析 1：静态回应池 vs AI 的精度-可靠性权衡

### 当前架构
L1(缓存) → L2(回应池54条) → L3(AI+1次重试) → L4(正则fallback)

### AI 并发测试数据重新解读

用户做了两次测试：
- 20次并发（200ms间隔）：67%成功，平均2283ms
- 50次并发（200ms间隔）：80%成功，平均2398ms

**关键问题：这测试模拟的是什么场景？** 

200ms间隔意味着前一个请求还没返回，下一个就发出去了。20次测试中，在约4秒内发出了20个并发请求。这是**峰值并发**，不是**日常使用**。

### 真实使用场景的并发分析

假设3个学员同时练习：
- 每人10轮对话
- 每轮间隔5-30秒（打字+思考时间）
- 总时长：约10分钟
- 峰值并发：最坏情况下2-3个请求同时到达（两人恰好同时点发送）
- AI调用总量：30次

在2-3个并发请求的场景下，AI成功率应远高于67%。因为低代码平台AI插件的瓶颈很可能是**瞬时并发上限**，而非**QPS上限**。

### 反转当前优先级：AI优先，回应池降级

```
当前（精度低，可靠性高）:
L1缓存 → L2回应池 → L3 AI → L4 fallback
          ↑ 大部分请求在这里结束，精度在L2就死了

建议（精度高，可靠性不降）:
L1缓存 → L2 AI(3秒超时) → L3 回应池 → L4 fallback
          ↑ AI成功=精准回应 | AI失败=回应池保障
```

### 论证

**为什么AI优先更优：**
1. 真实并发场景下AI成功率远高于67%（单用户顺序调用几乎100%成功）
2. AI能理解上下文（"你个人对下半年有什么想法"→ 理解"个人想法"vs"项目数据"的区别）
3. 54条静态回应覆盖9种意图，但真实管理者的语言远超9种
4. AI失败的降级路径清晰：3秒超时→回应池→fallback，用户感知不到延迟（3秒内有thinking indicator）

**为什么当前L2优先是错的：**
1. 回应池的精度天花板很低——意图分类是关键词匹配，"你个人对下半年有什么想法"被判为 `ask_opinion` → 拉到 TEST.other 的通用回应
2. 回应池命中后不调AI，意味着**大部分请求失去了个性化的机会**
3. 为了提升精度，必须不断增加回应条数——Abilitie说需要80条，但80条也覆盖不了所有自然语言变体

### 论证结论
**AI优先 + 3秒超时 + 回应池降级是最优解。** 精度来自AI，可靠性来自回应池。不牺牲精度换可靠性——两者都可以有。


## 分析 2：意图分类器的精度天花板

### 当前 classifyIntent 能做什么
```typescript
// 9条正则规则，优先级匹配
if (/来不及|只剩.*天|.../.test(text)) return 'pressure';
if (/加油|相信.*你们|.../.test(text) && text.length < 50) return 'vague_pep';
// ...
return 'other';
```

### 它能做到什么精度
用真实对话测试：你测试了三句话：
1. "你帮我看看最近数据怎么样" → 匹配 `talk_biz`（"数据"关键词）→ ✅ 分类正确
2. "最近项目情况如何" → 匹配 `ask_info`（"项目.*怎么|最近.*情况"）→ ✅ 分类正确
3. "你个人对下半年有什么想法" → 匹配 `ask_opinion`（"你有什么想法"→ 实际上"你个人...有什么想法"也触发了"你.*想法"）→ ✅ 分类基本对，但回应不精准

### 问题在哪
不是分类错误。是**回应池里的"ask_opinion→TEST"的回应太通用**。三个变体都是"砍户型/做减法/交叉推荐"相关的，没有一个回应"个人对下半年的想法"。

**根因：回应池的(mood, intent)组合覆盖了意图的"类型"，但没覆盖意图的"内容"。"征求意见"有无数种问法——问项目、问个人、问团队、问市场。每个方向的回应应该不同。**

### 解决方向
如果AI优先（分析1的结论），意图分类器就不需要做到"内容级"匹配——它只需要判断"有没有命中L1缓存"和"要不要做L3降级"。真正的个性化回应由AI生成。

**意图分类器降级为"缓存键生成器"+"降级回应选择器"。不需要精确分类到"个人想法的意见征求"vs"项目方向的征求意见"——AI会处理这个差异。**


## 分析 3：三种对话模式的引擎差异

### 什么是真正不同的

| | 管理陪练(narrative) | 训战陪练(training) | 业务陪练(drill) |
|---|---|---|---|
| NPC行为 | 情绪状态机+隐藏动机 | 场景NPC+知识点触发 | 标准化客户+可预测回应 |
| 引导者介入 | 轻声提醒(接住情绪) | 知识提示(MTP3·8) | 话术对比(漏了朝向) |
| 陷阱/检测 | 管理反模式(T01-T09) | 知识点遗漏检测 | 脚本合规检测 |
| 暂停机制 | "想想她为什么这样说" | "这个场景对应MTP哪个概念" | "标准话术是..." |
| 反馈重点 | 觉察+行为模式 | 知识薄弱点+老师信号 | 话术步骤完成率 |

### 什么是相同的

- 消息发送/接收
- AI调用（参数不同，但调用方式相同）
- 对话历史管理
- 缓存层
- UI渲染

### 如何实现模式差异
**不是写三个引擎。是让引擎读配置。**

```typescript
// 引擎入口 — 所有场景共用
async function respond(sceneId: string, userMsg: string, history: HistoryEntry[]): Promise<string> {
  const config = await loadSceneConfig(sceneId); // 从场景卡JSON加载
  
  // 这几步所有模式一样
  const cached = checkCache(userMsg, config.dialogue_mode);
  if (cached) return cached;
  
  // AI调用 — prompt由config决定
  const aiResp = await callAI(config.systemPrompt, history, 3_000);
  if (aiResp) { cache(userMsg, aiResp); return aiResp; }
  
  // 降级 — 回应池由config决定
  return getFromPool(userMsg, config.responsePool);
}

// 引导者 — 独立模块，按mode分发
function getFacilitatorHints(userMsg: string, config: SceneConfig, ctx: Context): Hint[] {
  switch (config.dialogue_mode) {
    case 'narrative': return getCoachingHints(userMsg, config.prompt_rules, ctx);
    case 'training': return getTrainingHints(userMsg, config.knowledge_points, ctx);
    case 'drill': return getDrillHints(userMsg, config.script_steps, ctx);
  }
}
```

### 论证结论
**引擎代码不改。场景差异全部通过场景卡JSON配置。** 这是BetterUp Content Studio的核心思想——"内容"（场景卡）和"引擎"（respond函数）严格解耦。新增场景=新建JSON文件，不碰引擎代码。


## 最终架构

```
对话智能体（~200行引擎代码，所有场景共享）
│
├── respond(sceneId, userMsg, history)
│   ├── L1: 缓存查找(mode-aware key)
│   ├── L2: AI调用(3秒超时) ← systemPrompt来自场景卡
│   ├── L3: 回应池降级 ← responsePool来自场景卡
│   └── L4: fallback ← fallbackTemplates来自场景卡
│
├── getFacilitatorHints(userMsg, config, ctx)
│   ├── narrative → 轻声提醒
│   ├── training → 知识提示
│   └── drill → 话术对比
│
└── detectEvents(userMsg, config, ctx)
    ├── narrative → 陷阱检测 + NPC状态推进
    ├── training → 知识点遗漏 + 决策点插入
    └── drill → 脚本合规 + 步骤完成

场景卡JSON（~500行，每个场景一个文件）
├── dialogue_mode: "narrative" | "training" | "drill"
├── systemPrompt: "你是NPC-周..."
├── responsePool: { WATCH: { talk_biz: [...] } }
├── prompt_rules: { traps: [...], whispers: [...], triggers: [...] }
├── success_criteria: [...]
└── feedback_modules: [...]
```
