# 低代码平台部署完整包 · 2026-07-21/22

> 包含全部更新：班级管理、引擎页优化、管理看板、场景分类、首页优化
> 保存为一个文件，方便查找和部署

---

## 一、数据库迁移（Step 1 · 先执行）

在低代码平台 PostgreSQL 管理工具中执行：

```sql
-- 班级表
CREATE TABLE IF NOT EXISTS coaching_classes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  class_name VARCHAR(100) NOT NULL,
  admin_user_id VARCHAR(50) NOT NULL,
  invite_code VARCHAR(20) UNIQUE NOT NULL,
  project_company VARCHAR(100),
  created_at TIMESTAMP DEFAULT NOW()
);

-- 班级-学员关联表
CREATE TABLE IF NOT EXISTS class_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  class_id UUID NOT NULL REFERENCES coaching_classes(id) ON DELETE CASCADE,
  user_id VARCHAR(50) NOT NULL,
  joined_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(class_id, user_id)
);

-- coaching_sessions 加班级外键
ALTER TABLE coaching_sessions ADD COLUMN IF NOT EXISTS class_id UUID REFERENCES coaching_classes(id);

-- 索引
CREATE INDEX IF NOT EXISTS idx_class_members_user ON class_members(user_id);
CREATE INDEX IF NOT EXISTS idx_class_members_class ON class_members(class_id);
CREATE INDEX IF NOT EXISTS idx_sessions_class ON coaching_sessions(class_id);
CREATE INDEX IF NOT EXISTS idx_classes_invite ON coaching_classes(invite_code);
```

---

## 二、引擎页替换（Step 2）

以下文件上传到低代码平台，替换原有引擎页。

### scene_TG001_引擎页.html（NPC-周对话页）

改动点：
- 新增语音输入（Web Speech API，点击麦克风开始/停止）
- 手机端适配（700px/400px 断点，字体缩放，44px 按钮，底部评分抽屉）
- 数据保护（`endConversation()` 保存 scores/traps/ctx 到 localStorage + 失败弹窗提示 + 后端异步同步）
- NPC 等待提示（AI 响应超 3 秒显示"NPC-周沉默了一会儿…"）

文件路径：`/Users/xzzsw127/Claude code/低代码平台部署_20260721/scene_TG001_引擎页.html`

### scene_PR2_引擎驱动版_v7.html（管理者B对话页）

改动点：
- 新增语音输入（同上）
- 手机端适配（同上）
- 数据保护（`endConversation()` 保存到 localStorage + mdp_sessions 统一存储 + 失败弹窗）

文件路径：`/Users/xzzsw127/Claude code/低代码平台部署_20260721/scene_PR2_引擎驱动版_v7.html`

---

## 三、管理看板替换（Step 3）

文件：`/Users/xzzsw127/Claude code/低代码平台部署_20260721/管理看板.html`

改动点：
- 顶部新增班级下拉框，按班级筛选数据
- 能力薄弱分布底部新增自动总结文字（"团队最需要提升的能力是 XX"）
- 导航栏统一为绿色 accent 风格

---

## 四、场景分类页上新（Step 4）

文件：`/Users/xzzsw127/Claude code/低代码平台部署_20260721/场景分类预览.html`

新增页面，三大板块结构：

```
⚡ 训战陪练（老师布置·课后练习）
  ├── TG-001 NPC-周 ✅ 可练
  ├── TG-002 NPC-张 🔜
  ├── TG-003 李经理 🔜
  └── MTP知识点练习 🔜

🎯 管理陪练（自主练习·管理发展）17个场景
  ├── ✨ 推荐给你（3张卡片）
  ├── 筛选栏（难度/条线/模块/仅可练）
  ├── 全部场景卡片（可练→待建→已归档）
  └── 自评弹窗（3题+自由输入模糊匹配）

💼 业务陪练（上传话术·自动生成）
  └── 销售/客服/自定义（待建）
```

---

## 五、首页优化（Step 5）

文件：`/Users/xzzsw127/Claude code/低代码平台部署_20260721/首页优化/HomePage.tsx`

用此文件替换低代码平台项目中的 `HomePage.tsx`。

改动点（13层嵌套 → 5层）：

| 优化项 | 具体改动 |
|--------|---------|
| 去外层 wrapper | `div.w-full` → `<>` fragment |
| 合并装饰层 | 5个独立光晕div → 1个 `radial-gradient` 多重背景 |
| 去内层动画包裹 | `motion.div > motion.div > h1` → `motion.h1` 直接挂动画 |
| 去 Button 包裹 | `Button asChild > Link` → `Link` 直接加样式 |
| 加移动端菜单 | 新增 hamburger 按钮 + 下拉菜单（`Menu`/`X` 图标） |

部署后注意确认 `lucide-react` 中 `Menu` 和 `X` 已导出。

---

## 六、其他新增页面

以下页面已建好，按需上传到低代码平台：

| 文件 | 位置 | 用途 |
|------|------|------|
| 练习记录.html | 项目根目录 | 个人练习历史+行动承诺追踪 |
| 整体架构预览.html | 项目根目录 | 内部参考 |
| 架构总览.html | 项目根目录 | 内部参考 |

---

## 部署验证清单

- [ ] 数据库迁移执行成功，coaching_classes 表可查询
- [ ] TG001 引擎页：语音输入可用、手机端正常、结束对话跳转反馈页
- [ ] PR2 引擎页：同上
- [ ] 管理看板：班级下拉框可见，数据筛选正常
- [ ] 场景分类预览页：三大板块显示正确，筛选+自评弹窗可用
- [ ] 首页：移动端 hamburger 可展开，动画流畅，无嵌套冗余

---

## 文件索引（所有文件在本地仓库的位置）

```
/Users/xzzsw127/Claude code/
├── 低代码平台部署_20260721/
│   ├── 班级管理_数据库迁移.sql
│   ├── scene_TG001_引擎页.html
│   ├── scene_PR2_引擎驱动版_v7.html
│   ├── 管理看板.html
│   ├── 场景分类预览.html
│   ├── 首页优化/
│   │   ├── HomePage.tsx
│   │   └── 首页优化预览.html
│   └── 部署说明.md
├── 练习记录.html
├── 整体架构预览.html
├── 架构总览.html
├── 陪练首页.html
├── 行动清单_20260721.md
├── 每日总结_20260721.md
└── 三智能体_对话记录.md
```
